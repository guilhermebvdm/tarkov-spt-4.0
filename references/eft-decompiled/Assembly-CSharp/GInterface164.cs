using ChartAndGraph;
using UnityEngine;

public interface GInterface164
{
	Object This();

	GameObject GetGameObject();

	void SetAxis(double scrollOffset, AnyChart parent, AxisBase axis, ChartOrientation axisOrientation, bool isSubDivisions);
}
