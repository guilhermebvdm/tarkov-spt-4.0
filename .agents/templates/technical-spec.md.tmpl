# {{NUM}} — {{TITLE}} · Spec Técnica

**Mod:** {{MOD}}
**Spec funcional:** [{{NUM}}-{{SLUG}}-01-spec.md]({{NUM}}-{{SLUG}}-01-spec.md)
**Criado:** {{CREATED_AT}}

> Fonte primária de verdade para qualquer assinatura, fórmula ou ponto de patch: [references/eft-decompiled/Assembly-CSharp/](../../../../references/eft-decompiled/Assembly-CSharp/). Toda referência ao código do EFT deve citar `arquivo.cs:linha`. Wiki SPT e fontes externas só como complemento.

## 1. Estratégia

[Tipo de patch (Prefix / Postfix / Transpiler / Replace), classe.método alvo no Assembly, e justificativa breve sobre alternativas descartadas.]

## 2. Pontos de patch

| Alvo (Assembly) | Tipo | Motivo |
|---|---|---|
| [`<arquivo>:<linha>`](../../../../references/eft-decompiled/Assembly-CSharp/<arquivo>#L<linha>) | Postfix | [O que faz] |

## 3. Novas propriedades F12 (BepInEx)

> Omitir esta seção se a feature não introduz `ConfigEntry`.

| Seção | Nome (EN) | Tipo | Padrão | Faixa | Avançado | Tooltip (pt-BR) |
|---|---|---|---|---|---|---|
| `<Section>` | `Property Name` | float | `1.0` | 0 a 2 | — | [Texto exato do tooltip] |

## 4. Arquivos do mod

| Arquivo | Ação | Resumo |
|---|---|---|
| `modded/Plugin.cs` | MODIFICAR | [O que muda em uma frase] |
| `modded/Patches/<Novo>Patch.cs` | CRIAR | [O que é] |

## 5. Stubs de código

> Blocos compiláveis com assinatura completa e corpo mínimo plausível. Cada referência a algo do EFT deve ter um comentário `// ref: Assembly-CSharp/<arquivo>:<linha>`. Para tipo ofuscado, o comentário pode nomear o conceito (`GClass680 = ABotProfileCreator`, nome 4.1 projetado — ver `docs/files-from-4.1/consolidated-mappings.txt`; não pinar, AP-09).

```csharp
// modded/Patches/<Novo>Patch.cs
using HarmonyLib;
using SPT.Reflection.Patching;

namespace <Namespace>.Patches;

public class <Novo>Patch : ModulePatch
{
    protected override MethodBase GetTargetMethod()
    {
        // ref: Assembly-CSharp/PlayerPhysicalClass.cs:288
        return AccessTools.Method(typeof(<Type>), "<method>");
    }

    [PatchPostfix]
    private static void Postfix(ref float __result)
    {
        // [Lógica mínima]
    }
}
```

## 6. Fluxo de dados

```
[A] usuário/jogador → [B] hook do mod → [C] cálculo do EFT → [D] output
```

[Explicar passo a passo, citando linhas tanto do mod quanto do Assembly.]

## 7. Riscos e dependências

- **Patches existentes em `modded/Patches/`** que podem conflitar: [lista]
- **Compatibilidade com outros mods** que tocam o mesmo sistema: [lista]
- **Ordem de inicialização:** [se relevante]

## 8. Checklist de implementação

- [ ] [Tarefa atômica 1]
- [ ] [Tarefa atômica 2]
- [ ] [Tarefa atômica 3]

## 9. Conformidade com skills (auto-checklist)

> Preenchido pelo `/create-technical-spec` ANTES de salvar. Cada linha: ✅ com evidência (seção desta spec ou `arquivo:linha`), ou **N/A + razão**. Linha ❌ → a spec não está pronta. Validado pelo `/review-technical-spec`. Taxonomia: [docs/technical/spt-antipatterns.md](../../../../docs/technical/spt-antipatterns.md).

| # | Check | Status | Evidência / razão |
|---|---|---|---|
| 1 | Lifecycle de raid: start hook + stop hooks idempotentes (`GameWorld.OnDestroy` + `BaseLocalGame.Stop`) — AP-01 | | |
| 2 | Filtro MainPlayer/Fika em todo patch que reage a ação de player — AP-02 | | |
| 3 | Alvos ofuscados/virtuais resolvidos por assinatura; TODOS os overrides auditados — AP-03 | | |
| 4 | Mudança de estado via API canônica do EFT; side-effects mapeados — AP-04 | | |
| 5 | Estado entre raids: raid1→exit→raid2 e alt-F4/morte/MIA cobertos | | |
| 6 | Semântica/defaults/faixas de cada ConfigEntry sem ambiguidade (incl. estado neutro) — AP-05 | | |
| 7 | Re-invocação de método patcheado tem reentry-guard/`ReversePatch` (sem recursão infinita) — AP-07 | | |
| 8 | Flags/caches de intercept validados contra o contexto atual após troca (arma/operação/tela) — AP-08 | | |

## Histórico

| Data | Evento |
|---|---|
| {{CREATED_AT}} | Spec técnica criada via `/create-technical-spec` |
