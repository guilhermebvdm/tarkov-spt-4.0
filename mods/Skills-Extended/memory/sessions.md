# Skills-Extended — Memória de Sessões

## Estado atual (snapshot ao fim da última sessão)

- Auditoria técnica de código completa realizada (28 achados, 6 dimensões, 5 sub-agentes paralelos): [`docs/relatorio-auditoria-codigo-01.md`](../docs/relatorio-auditoria-codigo-01.md). Causa raiz sistêmica identificada: `GameUtils.GetSkillManager()`/`GetPlayer()`/`GetProfile()` (`Plugin/Utils/GameUtils.cs:76-93`) são `[CanBeNull]` e sempre resolvem `Singleton<GameWorld>.Instance.MainPlayer` — nunca o dono real da ação patcheada. Afeta 11 dos 28 achados.
- Item [001](../backlog/001-corrigir-bugs-criticos-auditoria-01/) (5 dos 6 achados 🔴 críticos) — workflow completo até `/apply-code-review`, entregue v2.2.5. Testado in-game solo pelo usuário, confirmado funcionando.
- Item [002](../backlog/002-corrigir-cap-medicamento-instancia/) (6º achado crítico, `AUD-01-03`, desmembrado do 001 por exigir mudança arquitetural maior) — workflow completo até `/apply-code-review`, entregue v2.2.5 (mesmo build do 001). Testado in-game solo pelo usuário, confirmado funcionando.
- Item [003](../backlog/003-corrigir-achados-altos-auditoria-01/) (9 achados 🟠 Alto: `AUD-01-07` a `AUD-01-15`) — workflow completo até `/apply-code-review`, entregue v2.2.6. Compilado (Plugin + Common, 0 erros); **ainda não testado in-game** pelo usuário.
- Item [004](../backlog/004-corrigir-achados-restantes-auditoria-01/) (13 achados restantes 🟡/🔵/💡: `AUD-01-16` a `AUD-01-28`) — workflow completo até `/apply-code-review`, entregue v2.2.7. Compilado (Plugin + Common + Server, 0 erros); **ainda não testado in-game** pelo usuário. **Com este item, os 28 achados da auditoria 01 estão todos endereçados** (fechados ou aceitos como débito técnico explícito) — a auditoria como um todo está concluída.
- Versão atual do mod: 2.2.7 (`SkillsExtendedInfo.VERSION`, `Plugin.csproj`, `Server.csproj` sincronizados). Nada instalado em `E:/Tarkov Red Line` sem confirmação explícita prévia — build sempre via `dotnet build -c Release -o` redirecionado para pasta externa segura (`mods/Skills-Extended/builds/`), nunca `/compile-mod` neste mod.
- **Cuidado ao intercalar builds de Plugin e Server:** `Common.csproj` é referenciado por ambos; alternar `dotnet build` entre `-o mods/Skills-Extended/builds/` (Plugin) e `-o mods/Skills-Extended/builds/server/` (Server) pode fazer o cache incremental do Common não recopiar o `.dll` pra uma das duas pastas mesmo já tendo copiado antes (visto na prática: `SkillsExtendedCommon.dll` sumiu de `builds/server/` depois de recompilar só o Plugin). Sempre conferir os dois DLLs (`SkillsExtendedServerMod.dll` + `SkillsExtendedCommon.dll`) juntos em `builds/server/` antes de considerar o build do Server completo — recompilar o Server de novo resolve.
- **Comparativo com FIKA deve sempre usar `mods/FIKA/modded/`** (fork mantido por este repo), nunca `references/fika-plugin/` genérico — exigência explícita do usuário, aplicável a qualquer spec técnica/review futura deste mod que precise validar comportamento de rede/coop.

## Pendências / próximos passos conhecidos

- [P-1.3] (aberta 2026-09-07) Item 001, achado `CR-01-02` (rejeitado como dívida): 4 dos 6 overrides de `SmoothDoorOpenCoroutine` (`Switch`, `LootableContainer`, `DoorSwitch`, `BufferGateSwitcher`) nunca foram confirmados quanto a chamar `base.*` — mitigado pelo fail-safe de `DoorSoundPatch` (retorna som nativo se não achar o dono da interação), mas não verificado. Categoria: débito técnico, baixo risco.
- [P-2.1] (aberta 2026-09-07) Item 003, achado `AUD-01-08` (metade Recoil, rejeitado no code-review como `CR-01-01`): `weapon.Template.RecoilForceUp`/`RecoilForceBack` continuam mutando o template compartilhado por `TemplateId` — nenhuma property por-instância equivalente a `Weapon.ErgonomicsTotal` existe pra Recoil no Assembly (busca estática confirmou isso; não foi tentado profiling/logging em runtime). Em coop, o recuo de uma arma do mesmo modelo usada por outro jogador/bot ainda reflete a Weapon Systems skill do jogador local. Usuário decidiu explicitamente não reabrir isso nesta rodada ("ignora o recoil, pode seguir"). Categoria: débito técnico, candidato a investigação futura (via runtime, não leitura estática).
- [P-2.2] (aberta 2026-09-07) Item 003 entregue (v2.2.6) mas não testado in-game — validar tooltip de item médico com custo reduzido (`AUD-01-07`) e bônus de arma ativo desde o 1º frame da raid (`PA-01-01`). Categoria: bloqueador de fechamento total do item.
- [P-3.1] (aberta 2026-09-07) Item 004 entregue (v2.2.7) mas não testado in-game — validar cursor/input do minigame de arrombamento após mover a reasserção pra `OnEnable` (`AUD-01-28`), e ajuste de duração/chance do estimulante não "vazando" entre usos (`AUD-01-20` — prioridade alta, era um bug funcional real, não só alocação). Usuário decidiu deliberadamente testar os itens 003 e 004 juntos, de uma vez, em vez de por item. Categoria: bloqueador de fechamento total do item.

---

## 2026-09-07 16:22 (GMT-3) — Sessão 1: Auditoria completa + remediação dos 6 achados críticos (itens 001/002) + início do item 003

**Tema central:** Auditar o código do Skills-Extended de ponta a ponta após um crash real em produção (`MeleeSpeedPatch`), e remediar formalmente os achados mais graves via o fluxo de backlog do repo.

**Decisões-chave:**
- **Origem da auditoria:** um `NullReferenceException` real em `MeleeSpeedPatch.cs:30` (mod de terceiro, vendorizado com `original/`+`modded/`) estava causando um soft-lock no LoadAmmoAnim (mod próprio, corrigido na mesma sessão antes deste trabalho). Corrigido ad-hoc primeiro (v2.2.3, null-check em vez de `!`), depois auditado por completo.
- **Método de auditoria:** particionado em 6 dimensões técnicas, delegadas a 5 sub-agentes read-only em paralelo (mod grande, ~228 arquivos em 6 projetos: `Plugin/`, `Server/`, `Common/`, `Prepatcher/`, `FikaSync/`). Achados duplicados entre dimensões mesclados citando ambas as origens. Ref: [relatorio-auditoria-codigo-01.md §Metodologia](../docs/relatorio-auditoria-codigo-01.md).
- **Padrão sistêmico:** 11 dos 28 achados compartilham a mesma causa raiz (`GameUtils.GetSkillManager()`/`GetPlayer()`/`GetProfile()` sempre resolvem o `MainPlayer` local, nunca o dono real da entidade/ação patcheada) — documentado na tabela "Padrão sistêmico" do relatório antes dos achados individuais, para não corrigir os 11 pontos como bugs isolados sem reconhecer a causa raiz comum.
- **Item 001 vs 002:** dos 6 achados 🔴 críticos, o de "cap de medicamento" (`AUD-01-03`) foi desmembrado para um item próprio (002) por decisão explícita do usuário — sua correção exige mover o ponto de patch de um método **estático** utilitário para o método de **instância** que realmente conhece o dono do efeito, mudança arquitetural mais trabalhosa que os outros 5 achados (que são guards de null/instância mais diretos).
- **Item 001 — correções aplicadas:** `DoorSoundPatch` (som de porta só reflete Silent Ops quando a interação é do jogador local, via novo `DoorInteractionTrackerPatch` que registra o dono real por porta), `MovementContextSetSpeedLimitPatch` (guard `IsYourPlayer` + null-check, seguindo o padrão já correto de `CanWalkPatch`/`ProneMoveStatePatch` no próprio mod), `UpdateWeaponsPatch` (null-check a cada iteração da coroutine multi-frame, não só no topo), `CultistProductionPatch` e `GeneratePlayerScavPatch` (eliminação de `static` compartilhado entre requisições HTTP concorrentes — `__state` do Harmony em vez de campo estático). Asbuild: [001-...-05-asbuild.md](../backlog/001-corrigir-bugs-criticos-auditoria-01/001-corrigir-bugs-criticos-auditoria-01-05-asbuild.md).
- **Item 001 — achado descoberto no próprio code-review** (não estava na spec): o primeiro design de `CultistProductionPatch` (Postfix em `StartSacrifice`) podia reaplicar o desconto de tempo numa produção pré-existente se `StartSacrifice` saísse por um caminho de erro antecipado — corrigido movendo o alvo do patch para `RegisterCircleOfCultistProduction` (só é chamado quando uma produção nova é de fato criada). Ref: [001-...-04-code-review-01.md CR-01-01](../backlog/001-corrigir-bugs-criticos-auditoria-01/001-corrigir-bugs-criticos-auditoria-01-04-code-review-01.md).
- **Item 002 — correção aplicada:** novo `StimulatorHealthControllerTrackerPatch` captura o `HealthController` real (dono do efeito) a partir do método de instância `Stimulator.method_6` (resolvido por assinatura, não por nome literal — nomes `method_N` não são estáveis entre gerações de decompile), disponibilizando-o para `StimulatorApplyBuffPatch` resolver o `SkillManager` correto em vez de sempre o do `MainPlayer`. Asbuild: [002-...-05-asbuild.md](../backlog/002-corrigir-cap-medicamento-instancia/002-corrigir-cap-medicamento-instancia-05-asbuild.md).
- **Item 002 — achado no code-review:** a reflection nova de `StimulatorHealthControllerTrackerPatch` não tinha os mesmos guards de null que o resto do item — inconsistente com o próprio motivo do item existir (evitar exatamente esse padrão de risco). Corrigido: `GetTargetMethod()` trocado de `.First()` para `.FirstOrDefault()` + log de erro, property resolvida via método próprio checado antes de usar no Prefix. Ref: [002-...-04-code-review-01.md CR-01-01](../backlog/002-corrigir-cap-medicamento-instancia/002-corrigir-cap-medicamento-instancia-04-code-review-01.md).
- **Compilação sem `/compile-mod`:** Skills-Extended exigiu overrides de propriedades MSBuild (`TarkovManagedDir`, `TarkovBepInExCoreDir`, etc.) apontando pro install real do SPT (somente leitura, só para compilar), com `TarkovDir` redirecionado para uma pasta de descarte externa — evita que o `PostBuild` do `.csproj` (que copia DLLs incondicionalmente) escreva na pasta real do jogo. Usuário autorizou explicitamente essa abordagem via pergunta direta.
- **`BepInEx.cfg` `DumpAssemblies`:** precisou ser ativado temporariamente (usuário autorizou acesso ao arquivo) porque o Prepatcher do mod injeta enum/campo customizado via Mono.Cecil antes do jogo carregar, e o projeto do Plugin referencia o assembly já dumped/patched. Revertido para `false` após a compilação, como prometido.
- **FIKA como referência:** toda verificação de comportamento cooperativo (ex.: `FikaSync/`) deve comparar contra `mods/FIKA/modded/` (o fork mantido por este repo), nunca `references/fika-plugin/` genérico — pode divergir. Exigência explícita do usuário, repetida a agentes em execução.
- **Teste in-game:** usuário validou v2.2.5 solo (sem coop/headless disponível localmente) — resultado "bem tranquilo" aside de um erro `ArgumentOutOfRangeException` do SAIN (mod não relacionado), triado como fora de escopo e transformado num prompt de investigação separado para outra sessão (não pertence à memória deste mod).
- **Item 003 iniciado:** os 9 achados 🟠 Alto (`AUD-01-07` a `AUD-01-15`) agrupados num único item — cobrem mutação de template compartilhado (item médico + arma), gatilho de reprocessamento sem gating de raid, 4 pontos de leitura insegura/ambígua de skill (incluindo um bug de copy-paste isolado numa lista de reflection não utilizada), e 2 problemas de limpeza de estado (leak de assinatura de evento entre raids, dicionário de tentativas de fechadura sem limpeza no headless). Spec funcional criada, ainda sem review.

**Lições / hipóteses descartadas:**
- A spec técnica do item 002 assumiu inicialmente que `newSkillCap` (em `StimulatorApplyBuffPatch`) seria `float?`, propondo um `?? limits.Value.y`. O compilador provou isso errado (`CS0019`): o membro final da cadeia `?.` é `SkillManager.SkillBuffClass` — uma classe wrapper com conversão implícita para `float`, não um tipo valor — então a expressão não vira `Nullable<T>`. Corrigido revertendo para `Mathf.Clamp(value, limits.Value.x, newSkillCap)` (idêntico ao original nesse ponto). Lição: cadeias `?.` só produzem `Nullable<T>` quando o **último** membro acessado é tipo valor; se for referência com conversão implícita, o tipo da expressão continua sendo o da referência.
- Suspeita levantada durante o code-review do item 001 de que o mecanismo do `CultistProductionPatch` (mutar `pmcData.Hideout.Production` depois que `StartSacrifice` roda) poderia fazer a resposta HTTP não refletir o desconto — desconfirmada após ler o corpo completo de `CircleOfCultistService` no `spt-source`: `pmcData`/`Production` são o mesmo objeto por referência em toda a cadeia, mutado antes de qualquer serialização. Não virou achado (foi verificado e descartado antes de reportar).

**Atividade cronológica:**
1. Diagnosticado e corrigido bug original de municiamento silencioso (CLA + LoadAmmoAnim) — não é Skills-Extended, ver `mods/SPT-ContinuousLoadAmmo/` e `mods/LoadAmmoAnim/` para essa parte.
2. Crash em produção do `MeleeSpeedPatch` (Skills-Extended) identificado como causa do soft-lock do LoadAmmoAnim — corrigido ad-hoc (v2.2.3).
3. `/document-mod` rodado para Skills-Extended (gera `docs/`) em paralelo ao teste do usuário da correção anterior.
4. `/audit-mod-code` rodado (sem args, mod escolhido via pergunta) — 5 sub-agentes paralelos, 6 dimensões, relatório consolidado com 28 achados.
5. Resumo leigo do motivo/achados da auditoria fornecido ao usuário (para repassar a um amigo).
6. Usuário decide: desmembrar achado do medicamento (`AUD-01-03`) em item 002 separado; headless como corner case obrigatório do item 001.
7. Item 001: `/add-backlog-item` → `/create-spec` → `/review-spec` → `/create-technical-spec` → `/review-technical-spec` → `/code-mod` → `/code-review` → `/apply-code-review`. Compilado 0 erros. v2.2.4 → v2.2.5.
8. Item 002: mesmo fluxo completo. Compilado 0 erros (após corrigir a suposição errada de nullable). v2.2.4 → v2.2.5 (mesmo bump do 001).
9. Usuário testou v2.2.5 in-game solo — confirmou funcionamento correto, reportou 1 erro não relacionado (SAIN).
10. Prompt de investigação separado escrito para o crash do SAIN (fora do escopo deste mod, não gravado aqui).
11. Item 003 criado (`/add-backlog-item` + `/create-spec`) agrupando os 9 achados 🟠 Alto — spec funcional escrita, aguardando `/review-spec`.

**Cross-refs:**
- Trabalho paralelo no mesmo dia em outros mods: `mods/SPT-ContinuousLoadAmmo/` (fix do bug original de municiamento) e `mods/LoadAmmoAnim/` (mesmo fix + hardening de recovery).
- Investigação do crash do SAIN reportado durante o teste — não é trabalho deste mod; prompt de investigação preparado para sessão separada, sem registro aqui.

---

## 2026-09-07 19:16 (GMT-3) — Sessão 2: Item 003 completo (9 achados 🟠 Alto) — spec, build, compilação e code-review

**Tema central:** Levar o item 003 (9 achados 🟠 Alto da auditoria 01) do fim do `/create-spec` até entregue — `/review-spec` → `/create-technical-spec` → `/review-technical-spec` → `/code-mod` → compilação → `/code-review` → `/apply-code-review`.

**Decisões-chave:**
- **`AUD-01-07` (custo de item médico compartilhado):** resolvido por completo, incluindo o tooltip. A investigação no Assembly achou um mecanismo do próprio EFT pra isso — `Item.AddOrReplaceAttribute` + `Item.Replacements` ([Item.cs:360-390,897-908](../../../references/eft-decompiled/Assembly-CSharp/EFT.InventoryLogic/Item.cs)) — que permite recriar a attribute do tooltip apontando pro custo já ajustado, sem precisar de Transpiler no construtor base. Usuário escolheu investigar em vez de aceitar a regressão cosmética que eu tinha inicialmente levantado.
- **`AUD-01-08` (ergonomia/recuo compartilhados) — resolvido pela metade, por decisão do usuário:** Ergonomia tem fix completo e limpo via `Weapon.ErgonomicsTotal` ([Weapon.cs:773](../../../references/eft-decompiled/Assembly-CSharp/EFT.InventoryLogic/Weapon.cs#L773)), confirmadamente lida pela ergonomia real de gameplay em [Player.cs:12845](../../../references/eft-decompiled/Assembly-CSharp/EFT/Player.cs#L12845). Recuo **não tem** equivalente — `RecoilForceUp` não tem property de instância alguma no Assembly, e `RecoilForceBack` é só um repasse direto do template. Usuário aceitou explicitamente manter Recoil mutando o template compartilhado como débito técnico (`[P-2.1]`), tanto na `/review-technical-spec` quanto reafirmado no `/code-review` ("ignora o recoil, pode seguir").
- **`AUD-01-10` (cap de skill do dono errado):** resolvido reaproveitando um hook que o próprio mod já tinha (`SkillManagerConstructorPatch`, que roda pra QUALQUER `SkillManager` criado, não só o do MainPlayer) — adicionado um `ConditionalWeakTable<AbstractSkillClass, SkillManager>` pra mapear cada skill ao seu dono real sem segurar referência forte (evita leak equivalente ao `AUD-01-14`).
- **`AUD-01-09`/`AUD-01-14` combinados:** ao investigar o achado do leak de evento (`AUD-01-14`), descobri que a cadeia de herança de `GameWorld` só tem 2 subclasses concretas reais (`ClientLocalGameWorld`, solo; `ClientNetworkGameWorld`, coop/Fika/headless) e AMBAS chegam em `GameWorld.OnDestroy()` sem nenhum override quebrando a cadeia — fechei esse achado por completo (não como débito mitigado, diferente do que a spec técnica antecipava). Aproveitei o mesmo hook novo (`OnGameEndedPatch`) pra também limpar os caches de arma do `AUD-01-09`.
- **Revisão técnica (rodada 01) encontrou 2 gaps reais** que a spec técnica original não cobria: (1) o gate "só roda em raid" do `AUD-01-09` podia deixar o bônus de arma inativo nos primeiros instantes da raid (antes, navegar o hideout pré-raid "pré-cozia" o efeito no template compartilhado — com o gate, isso parou de acontecer) — resolvido chamando o reprocessamento também a partir do `OnGameStartedPatch`; (2) o fix do `AUD-01-12` removia a chamada redundante mas não cobria o corner case de "handler dispara numa janela de transição de cena" que a própria spec funcional deste item exigia — resolvido com guard de nulidade explícito em `Player`.
- **Compilação:** Plugin + Common compilaram com 0 erros de código (`dotnet build` redirecionado pra `mods/Skills-Extended/builds/`, mesma técnica de 001/002 — nada tocou `E:/Tarkov Red Line`). O erro `MSB3030` do PostBuild apareceu de novo (esperado, documentado desde a sessão anterior).
- **Code-review encontrou 1 achado real:** o critério de aceite da spec funcional (linha 28) bundla ergonomia+recuo como uma coisa só, mas só ergonomia foi resolvida — um leitor futuro que só olhasse a spec funcional não saberia que Recuo ficou de fora. Usuário optou por não editar a spec funcional, aceitando a inconsistência documental junto com o débito técnico em si.

**Lições / hipóteses descartadas:**
- Investigação inicial assumiu que trocar `HealthEffectsComponent.IHealthEffect` por um wrapper resolveria tudo de graça, incluindo o tooltip — **falso**: `Item.CreateAttributesFromDictionary` roda no construtor base ANTES do nosso Postfix, e as lambdas de UI capturam o objeto `GClass1443` original por referência, não por lookup dinâmico. Só descobri isso ao ler `Item.cs:930-948` com atenção; a correção exigiu um segundo mecanismo (`AddOrReplaceAttribute`) além da troca do wrapper.
- Cogitei estender o mesmo mecanismo por-instância de Ergonomia (`ErgonomicsTotal`/`ErgonomicsDelta`) pra Recoil — descartado: `RecoilDelta`/`RecoilTotal` existem no Assembly ([Weapon.cs:767,771](../../../references/eft-decompiled/Assembly-CSharp/EFT.InventoryLogic/Weapon.cs#L767-L771)) mas nenhum consumidor real foi encontrado pra eles (nem gameplay, nem tooltip — ambos leem `Template.RecoilForceUp`/`RecoilForceBack` diretamente). Diferente de Ergonomia, não existe uma camada de indireção por-instância já pronta pra Recoil.

**Atividade cronológica:**
1. `/review-spec` — 2 gaps de critério de aceite + 3 corner cases adicionados (cap de skill sem dono definido, reinício de processo vs. cache de gating, arma nova sem mudança de nível, janela de transição de cena no handler de XP médico).
2. Memória do mod criada pela primeira vez (`sessions.md` não existia) a pedido do usuário, cobrindo a Sessão 1 inteira retroativamente.
3. `/create-technical-spec` — investigação extensa no Assembly pros 9 achados; 2 pontos (`AUD-01-07`, `AUD-01-08`) revelaram limitações arquiteturais reais não previstas pela auditoria original.
4. Usuário decidiu: investigar mais o tooltip do item médico (não aceitar a regressão) e aceitar o Recoil como está.
5. `/review-technical-spec` — 4 achados (`PA-01-01` a `PA-01-04`), todos aceitos e aplicados diretamente na spec técnica.
6. `/code-mod` — 10 arquivos modificados/criados em `mods/Skills-Extended/modded/`; 1 achado extra descoberto durante o build (campo `ergo` órfão em `OrigWeaponValues`, removido); verificação residual do `AUD-01-14` fechada por completo (não só mitigada).
7. Compilação Release via `dotnet build` redirecionado — 0 erros de código, confirmado que nada foi escrito em `E:/Tarkov Red Line`.
8. `/code-review` — 1 achado (`CR-01-01`, gap documental entre spec funcional e débito técnico de Recoil).
9. `/apply-code-review` — usuário rejeitou o achado ("ignora o recoil, pode seguir"); nenhuma mudança de código nesta rodada. Item 003 marcado 🟢 no `mod-backlog.md`.

**Pendências abertas nesta sessão:**
- [P-2.1] (aberta 2026-09-07) Recoil de arma (`AUD-01-08` parcial) continua mutando template compartilhado — débito técnico aceito explicitamente pelo usuário duas vezes (spec técnica e code-review). Categoria: débito técnico.
- [P-2.2] (aberta 2026-09-07) Item 003 entregue mas não testado in-game — validar tooltip de item médico e bônus de arma no primeiro frame da raid. Categoria: bloqueador de fechamento total do item.

**Cross-refs:**
- Resolve pendência [P-1.1] da Sessão 1 (item 003 completo).

---

## 2026-09-07 20:10 (GMT-3) — Sessão 3: Item 004 completo (13 achados restantes) — auditoria 01 fechada por completo

**Tema central:** Fechar a auditoria 01 inteira agrupando os 13 achados restantes (6 🟡 Médio, 5 🔵 Baixo, 2 💡 Otimização) num item 004 e levá-lo do zero até entregue.

**Decisões-chave:**
- **`AUD-01-20` reclassificado de "alocação evitável" pra bug funcional real:** a auditoria original descrevia como desperdício de alocação (`.Clone()` descartado), mas ao ler `SkillManagerExt.AdjustStimulatorBuff` por completo, confirmei que o método muta o objeto recebido e não retorna nada — como os dois call-sites clonavam e descartavam o clone, **o bônus de duração/chance do Field Medicine sobre estimulantes nunca tinha efeito nenhum**, nem no jogo nem no tooltip. Corrigido mutando `__result`/`__instance` diretamente. Achei evidência indireta forte de que isso é seguro (não reintroduz o padrão de template compartilhado do `AUD-01-07`) no caller real em [ActiveHealthController.cs:2587-2597](../../../references/eft-decompiled/Assembly-CSharp/EFT.HealthSystem/ActiveHealthController.cs#L2587-L2597) — mas `InjectorBuff`/`BuffSettings` vivem em `ItemComponent.Types.dll` (não decompilada neste repo), então ficou um `TODO confirmar` residual com prioridade alta de teste manual.
- **`AUD-01-16` (velocidade de golpe) resolvido pela raiz, não só mitigado:** diferente do que a auditoria sugeria como alternativa mais barata (guard "só aplica se MainPlayer"), usei `GameWorld.AllAlivePlayersList` + `Player.HandsAnimator` pra achar o dono real de qualquer golpe (jogador local, bot, ou peer Fika) e usar a skill DELE, não sempre a do MainPlayer local.
- **Risco identificado na sugestão original da auditoria pro `AUD-01-21`:** cachear o menu de interação de porta "por porta" (ignorando o `owner`) reintroduziria o mesmo bug sistêmico do resto da auditoria — em coop, dois jogadores olhando a mesma porta reusariam o delegate vinculado ao primeiro. Ajustei pra memoizar só a parte cara e owner-independente (checagem de inventário de lockpicks) por `Time.frameCount`, mantendo os handlers frescos por chamada.
- **Verifiquei `ISptLogger<T>.Debug(string, Exception ex = null)` via reflection direta na DLL real do SPT** (PowerShell `Assembly.LoadFrom` + `GetMethods()`) antes de usar no fix do `AUD-01-25` — a spec técnica tinha assumido a assinatura sem confirmar; o pacote NuGet local (`~/.nuget/packages/sptarkov.server.core/`) permitiu confirmar sem depender do decompiled dump (que só cobre o cliente EFT, não pacotes do servidor SPT).
- **Code-review encontrou 1 achado real, já aplicado:** o cache de delegate criado pra corrigir `AUD-01-19` (evitar alocar closure a cada quadro de bruços) retinha o `Player` da raid anterior sem limpeza — mesma classe de leak do `AUD-01-14` (item 003), só que numa escala bem menor. Resolvido reaproveitando o `OnGameEndedPatch` que o item 003 já tinha criado, em vez de inventar um novo ponto de limpeza.
- **Compilação:** Plugin + Common + Server compilaram com 0 erros de código (mesma técnica de build redirecionado dos itens anteriores — Server usa `PackageReference` NuGet, só precisou redirecionar `TarkovDir` pro `CopyToServer` PostBuild, não pros `HintPath` de referências como o Plugin).
- **Com este item, os 28 achados da auditoria 01 (`docs/relatorio-auditoria-codigo-01.md`) estão todos endereçados** — 15 corrigidos por completo nos itens 001-003, 12 corrigidos por completo neste item, e 1 (`AUD-01-08` metade Recoil) aceito como débito técnico explícito. A auditoria como projeto está concluída; o que resta é só validação manual in-game (itens 003 e 004).

**Lições / hipóteses descartadas:**
- Nenhuma hipótese testada e descartada nesta sessão — diferente das sessões anteriores, os 13 achados desta rodada eram todos suficientemente bem descritos pela auditoria original pra não exigir descobrir um caminho alternativo no meio do processo (exceto o ajuste de escopo do `AUD-01-21`, já coberto acima como decisão-chave, não uma hipótese descartada).

**Atividade cronológica:**
1. `/add-backlog-item` + `/create-spec` — spec funcional criada agrupando os 13 achados.
2. `/review-spec` — 1 corner case adicionado (jogador removido do mundo entre abrir o minigame de arrombamento e o próximo uso da skill).
3. `/create-technical-spec` — investigação no Assembly pros 13 achados; descoberta do bug real do `AUD-01-20` durante a leitura de `SkillManagerExt.AdjustStimulatorBuff`.
4. `/review-technical-spec` — 0 achados formais; reforcei a evidência do `AUD-01-20` (caller real encontrado) e apliquei direto na spec antes de gerar a review.
5. `/code-mod` — 11 arquivos modificados em `mods/Skills-Extended/modded/`; confirmei a assinatura de `ISptLogger<T>.Debug` via reflection na DLL real antes de usá-la.
6. Compilação Release (Plugin + Common + Server) via `dotnet build` redirecionado — 0 erros de código.
7. `/code-review` — 1 achado (`CR-01-01`, retenção de `Player` entre raids no cache novo do `AUD-01-19`).
8. `/apply-code-review` — achado aplicado; recompilado, 0 erros. Item 004 marcado 🟢 no `mod-backlog.md`.

**Pendências abertas nesta sessão:**
- [P-3.1] (aberta 2026-09-07) Item 004 entregue mas não testado in-game — ver topo (validar cursor do minigame de arrombamento e o fix do bug do estimulante). Categoria: bloqueador de fechamento total do item.

**Cross-refs:**
- Resolve pendência [P-1.2] da Sessão 1/2 (13 achados restantes agora todos endereçados).
- Consolida [P-2.2] (validação do item 003) — os dois itens serão testados juntos por decisão do usuário.
