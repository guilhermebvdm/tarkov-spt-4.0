# Backlog — CustomClasses

> Índice de itens de backlog. Cada linha aponta para uma pasta `NNN-<slug>/` com a spec funcional, técnica e revisões. Roadmap derivado do plano aprovado em 2026-06-07.

| # | Título | Resumo | Pasta | Status |
|---|---|---|---|---|
| 078 | Caçador pesado sem motivo | Caçador aparenta sobrepeso/lentidão com apenas 31.7 kg (bem abaixo do limiar vanilla ~58.5 kg) — investigar vazamento de drawback (Overladen?) ou patch de velocidade do item 074. | [078-cacador-pesado-sem-motivo/](./078-cacador-pesado-sem-motivo/) | ⚪ |
| 001 | Scaffold + 1 classe (walking skeleton) | Projeto server C# que registra 1 classe como edition no launcher (`DescriptionLocaleKey` + locale `en`) com só skills estáticas. Prova o mecanismo de injeção de edition ponta a ponta. | [001-walking-skeleton/](./001-walking-skeleton/) | 🟢 |
| 002 | Schema de classe + loader multi-classe | **Um arquivo JSON por classe** em `Server/config/classes/`, lido **dinamicamente** → injeta N editions (soltar um `.json` = nova classe). Schema inicial (id, base, skills); **cresce a cada item** (itens/equip/skins/mults nos 003-008) até configurar *tudo* por classe. Porta recipe/anchor/balance do RZ. | [002-class-schema-loader/](./002-class-schema-loader/) | 🟢 |
| 003 | Itens + hideout + 10 classes reais | Capacidade de itens (stash/equipado/compostos via `ParentId/SlotId` + validador) **e hideout** (estações iniciais). **Conteúdo:** as 10 classes do RZ recriadas com **mesmos itens+skills** (agora equipados/compostos) + estação de hideout. Migração trazida do 007. | [003-starting-items/](./003-starting-items/) | 🟢 |
| 004 | Outfits por classe | `Customization` (Head/Body/Feet/Hands) + `Suits` via `databaseService.GetCustomization()`. | [004-outfits/](./004-outfits/) | 🟢 |
| 005 | Multiplicadores de skill por classe (client) | Multiplicadores por skill (buff/debuff) escalando o ganho de XP (sem distribuição dinâmica). Server serve config por rota; client BepInEx escala em runtime. **+ Exibir o multiplicador na tela de Skills** (no tooltip e/ou na listagem de cada skill, **mesma UX/layout** já usada lá): prefixo `+` se >1, `-` se <1; para `1` pular ou deixar bem sutil. Reusar padrão dos patches de UI de skill dos mods de referência (`SkillTooltipPatch`/`SkillPanelPatch`). | [005-skill-multipliers/](./005-skill-multipliers/) | 🟢 |
| 006 | Compat opcional com Skills-Extended | Soft-detect; multiplicadores p/ skills do SE só quando ele existe; no-op + aviso se ausente. (SE revive `ESkillId` vanilla → mecanismo já suporta.) Exemplo: Médico (FirstAid/FieldMedicine). | [006-skills-extended-compat/](./006-skills-extended-compat/) | 🟢 |
| 007 | Aposentar RZCustomProfiles | CustomClasses tornado **autossuficiente**: anchor vendored em `scripts/anchor-items.json` (zero dependência build/runtime do RZ). RZ desabilitado no server install (fora de `user/mods`) e **mantido no repo como aposentado** (decisão: deixar como está). Sem clobber, pois o RZ não roda mais. Item operacional (sem fluxo formal de spec). | — | 🟢 |
| 008 | i18n (multilíngue pt-BR/en) | Descrição de edition por idioma (segue a língua do servidor, fallback en; `description` string OU `{en,pt}`) + seletor de língua no F12 (default English) p/ os textos in-game. | [008-i18n/](./008-i18n/) | 🟢 |
| 009 | Ocultar edições vanilla no launcher | Config JSON p/ esconder edições da criação de perfil. Default: ocultar Standard/Left Behind/Prepare To Escape/Edge Of Darkness/Unheard/Tournament/SPT Easy start; manter SPT Developer + SPT Zero to hero + as classes do mod. (`CreateNewProfileTypesBlacklist`; v1-only) | [009-ocultar-edicoes-vanilla/](./009-ocultar-edicoes-vanilla/) | 🟢 |
| 010 | UI dos multiplicadores de skill | Refino visual do 005: borda colorida no ícone da skill (verde=buff/vermelho=debuff), seta+`±X%` à direita do nome (no lugar da seta azul) e tooltip dedicado "…devido à Classe **\<Nome\>**". Requer expor o nome da classe/edition ao client. | [010-ui-multiplicadores-skill/](./010-ui-multiplicadores-skill/) | 🟢 |
| 011 | Infra de identidade visual da classe | Base p/ 012/013: server expõe nome+ícone(PNG)+cor(hex) por classe ao client (estende `ClassDefinition` + rota); client carrega o PNG e monta o "selo" (ícone+nome colorido) reutilizável; `/compile-mod` passa a entregar assets ao client; placeholders genéricos. | [011-infra-identidade-visual/](./011-infra-identidade-visual/) | 🟢 |
| 012 | Identidade da classe no menu + tela de Skills | Ícone+nome da classe no menu principal (integra ao Menu-Overhaul; canto fixo sem ele) e no topo da tela de Skills (`SkillsAndMasteringScreen`). Usa o selo do 011. | [012-identidade-menu-skills/](./012-identidade-menu-skills/) | 🟢 |
| 013 | Botão SKILLS na navegação do menu | Botão "SKILLS" abaixo de "CHARACTER" que abre direto a tela de Skills. **BUG (06-fix-01):** não aparece com o Menu-Overhaul (clone órfão — MO reposiciona botões por nome). | [013-botao-skills-navegacao/](./013-botao-skills-navegacao/) | 🟡 |
| 014 | Mensagem de level-up de skill (buff/debuff) | Customiza a notificação "X skill leveled up to Y" das skills com multiplicador da classe: buff → `EASILY` (verde) + `;)`; debuff → `FINALLY` (vermelho). Skill sem multiplicador → mensagem vanilla. | [014-mensagem-levelup-skill/](./014-mensagem-levelup-skill/) | 🟢 |
| 015 | Identidade da classe no nome do jogador (deploy/character/online) | Customiza o widget comum `ChatSpecialIcon` (ícone "person" + nome) onde o **jogador local** aparece — deploy, tela de character, lista online: ícone da classe + nome colorido (gradiente) + título da classe. Mesma "variável" em vários lugares. Selo do 012 off por padrão. | [015-identidade-deploy/](./015-identidade-deploy/) | 🟢 |
| 016 | Classe "Peladão" | Classe nova: base `SPT Zero to Hero` (sem buff/debuff/skillMultipliers), nasce pelado + kit mínimo. **Placeholder funcional** (skin havaiana/descrição/cor) — conteúdo a revisar pelo usuário. | [016-classe-peladao/](./016-classe-peladao/) | 🟢 |
| 017 | BUG: customização (skin) não persiste no perfil | **Causa = bug do SPT core** (`ProfileFixerService` reseta Body/Hands/Feet válidos p/ default no `/client/game/start` — lógica invertida). Afeta qualquer skin, não só as classes. **Fix entregue em mod server dedicado** [`mods/CustomizationPersistenceFix`](../../../CustomizationPersistenceFix/) (patch Harmony). A validar in-game. | [017-customizacao-nao-persiste/](./017-customizacao-nao-persiste/) | 🟢 |
| 018 | Doc canônica do schema de classe | `docs/class-schema.md`: 100% dos campos do `ClassDefinition` (incl. `displayName`), semântica dos builders, validações do loader (refs por símbolo), limites SE. Reconcilia `exampleClass.jsonc`. Alimenta as specs do épico do editor (019–029). *(Editor — Wave W0)* | [018-schema-doc-canonica/](./018-schema-doc-canonica/) | 🟢 |
| 019 | Guard rails de config (anti-clobber + sync) | Anti-clobber no `compile-mod.sh` (diff repo×install de `config/classes/` + `--force-config`), script/skill `/sync-classes` (install→repo com preview), freeze do gerador (bootstrap-only), cópia genérica de `wwwroot/` p/ server. Pré-condição de qualquer save do editor. *(Editor — Wave W1, merge primeiro)* | [019-config-guard-rails/](./019-config-guard-rails/) | 🟢 |
| 020 | Infra web Blazor (padrão Skills-Extended) | csproj → `Sdk.Web` + `SPTarkov.Server.Web` 4.0.2 (compat c/ server 4.0.13 a verificar no boot), `IModWebMetadata`, `Web/` + `wwwroot/`, layout MudBlazor, home smoke. Rotas: páginas `@page "/customclasses/..."`; estáticos `/CustomClasses-Server/`. Ícones de classe no `wwwroot/icons/`. *(Editor — Wave W1, após 019; deps: 019)* | [020-infra-web-blazor/](./020-infra-web-blazor/) | 🟢 |
| 021 | ClassRegistrar (dry-run) + ClassEditorService | Extrai de `RegisterClass` um pipeline `Validate/Build` **puro** (diagnósticos) + `Commit/Remove` (registries ganham Remove/enumeração); `OnLoad` usa o mesmo pipeline. `ClassEditorService`: lê/escreve `.jsonc` do install c/ backup `.bak` + audit; save → hot-apply build-then-swap (perfil novo sem restart). *(Editor — Wave W1; deps: 018 soft)* | [021-class-registrar-editor-service/](./021-class-registrar-editor-service/) | 🟢 |
| 022 | Catálogo de itens + custo (port RZ) | `CatalogService` sobre o DB vivo (nomes/preços/presets/customization/editions). `CostService`: port fiel da fórmula RZ (SKILL_MULTS/BASELINE 15/budget 28–32 + `loadoutTotalRub`); pesos derivados p/ skills do SE (mecânica de upagem) + fallback por categoria; XP-mults fora do custo. Paridade só no custo de skills. *(Editor — Wave W1)* | [022-catalog-e-custo/](./022-catalog-e-custo/) | 🟢 |
| 023 | Pickers de item (MudBlazor) | Componentes reutilizáveis: item picker (busca/categoria/slot, virtualização), preset/ammo/customization pickers. Spike de imagem de item (estáticos SPT vs tarkov.dev vs texto) c/ decisão registrada. *(Editor — Wave W2; deps: 020, 022)* | [023-item-picker-ui/](./023-item-picker-ui/) | 🟢 |
| 024 | Viewer de classes (lista + detalhe read-only) | Lista lendo os **arquivos** via `ClassEditorService` (status registrada/disabled/inválida + diagnósticos do dry-run, custos); detalhe read-only completo (loadout resolvido, badges SE, breakdown de custo). *(Editor — Wave W2; deps: 020, 021, 022)* | [024-class-viewer/](./024-class-viewer/) | 🟢 |
| 025 | Edição de campos simples + outfit | Shell de abas + edição: displayName/description en-pt, cor, enabled, baseEdition, iconFile (preview), skills, mults, hideout, outfit (picker 023). Custo ao vivo + budget. `name` read-only (rename = duplicar). Save c/ hot-apply. **Fecha o MVP.** *(Editor — Wave W3; deps: 021–024)* | [025-edit-campos-simples/](./025-edit-campos-simples/) | 🟢 |
| 026 | Editor de loadout equipado | `ItemSpecEditor` compartilhado (preset/mods tree/ammo/contents recursivos) + aba Equipado (EquipmentSlots → ItemSpec), compat via `_props.Slots`, dry-run do pipeline antes do save. *(Editor — Wave W4; deps: 023, 025, 021)* | [026-edit-equipado/](./026-edit-equipado/) | 🟢 |
| 027 | Criar / duplicar / deletar classe | Criar (template mínimo), duplicar (caminho oficial de rename), deletar/desabilitar c/ hot-remove + confirmação (aviso de perfis existentes). *(Editor — Wave W4; deps: 021, 024, 025)* | [027-criar-duplicar-deletar/](./027-criar-duplicar-deletar/) | 🟢 |
| 028 | Editor de inventário (stash) | Aba Stash: lista plana de ItemSpec (reusa `ItemSpecEditor` c/ `AllowCount`), add/duplicar/remover, totais + valor ₽ ao vivo, dry-run do `GridPacker` (`CostService.CheckStashCapacity`) contra a grade da `baseEdition` p/ aviso de capacidade (não bloqueia). Sem grid visual (packing é runtime). Validado no browser real (fits 54% / overflow / discard). *(Editor — Wave W5; deps: 026, 025)* | [028-edit-stash/](./028-edit-stash/) | 🟢 |
| 029 | Docs e fechamento do editor | README (acesso, fluxo install↔repo, gerador congelado) + os 4 limites (hot-apply só perfil novo; perfis existentes imutáveis; comentários `.jsonc` perdidos no save; rename=duplicar) + smoke ponta a ponta. Entregue: `docs/class-editor.md` + README atualizado. *(Editor — Wave W5; deps: todos 018–028)* | [029-docs-e-fechamento/](./029-docs-e-fechamento/) | 🟢 |
| 030 | Sidebar persistente de classes | Drawer vira sidebar de classes (ícone tingido + nome na cor + custo + **dot de status** inválida/disabled/budget + **filtro por nome**), **1 clique** troca de classe preservando a vista (detail→detail, edit→edit; inválida cai no detail); **guard de unsaved changes** (Save/Discard/Cancel — nunca descarta em silêncio); `ListClassSummaries()` leve (sem dry-run por render). Port da UX do viewer de perfis do RZ. *(UX — Wave UX-W1)* | [030-sidebar-classes/](./030-sidebar-classes/) | 🟢 |
| 031 | Skills em ordem canônica (componente) | `SkillMaster` (ordem fixa Ph→M→C→P + seção SE; contagem derivada de `SkillWeights.cs`, sem números mágicos) + `SkillCanonicalList.razor`: TODAS as skills sempre na mesma posição, nível 0 esmaecido, barras por categoria, custo inline, chips ±% de XP. Modo read-only (detalhe) e edit (nível direto na linha — substitui o "Add skill"); zeros pré-existentes no JSON preservados. *(UX — Wave UX-W1)* | [031-skills-ordem-canonica/](./031-skills-ordem-canonica/) | 🟢 |
| 032 | Matriz de skills (classes × skills) | Página `/customclasses/skills`: heatmap skills (linhas, ordem canônica) × classes (colunas, header vertical na cor; **disabled esmaecidas c/ toggle**) — port do `profiles-skills.html` + rodapé com custo por classe, toggle de multiplicadores, célula clicável → classe. Comparação completa com **0 cliques**. *(UX — Wave UX-W2; deps: 030, 031)* | [032-matriz-skills/](./032-matriz-skills/) | 🟢 |
| 033 | Detalhe single-screen (dashboard) | `ClassDetail` sem expansion panels: header com badges (custos/status/ações), coluna esquerda skills canônicas+mults+hideout, coluna direita equipado+stash — **0 expansões/cliques**, densidade compacta (CSS local 12-14px). Single-screen completo fecha no 034 (stash ainda textual aqui). *(UX — Wave UX-W2; deps: 030, 031)* | [033-detalhe-single-screen/](./033-detalhe-single-screen/) | 🟢 |
| 034 | Loadout visual (gear + stash + tooltip) | `GearPanel` (slots estilo Tarkov com ícone tarkov.dev dimensionado por `_props.Width/Height`) + `StashPanel` (grid de ícones agrupado por categoria, badge qty, subtotal ₽) + tooltip hover (nome/tamanho/preço). Aba Stash do edit ganha **agrupamento por categoria + filtro**. Fecha a meta single-screen do 033. Degrada pra texto offline. *(UX — Wave UX-W3; deps: 033)* | [034-loadout-visual/](./034-loadout-visual/) | 🟢 |
| 035 | Densidade global + redução de cliques | `Dense=true` em tudo, Edit direto da lista/sidebar, **colunas ordenáveis** na lista, aba ativa preservada ao trocar de classe, `Ctrl+S`, célula da matriz → edit, **preferências persistidas** (localStorage: drawer/vista/aba/ordenação/toggles), passada de regressão visual Chrome MCP (incl. re-medição dos tempos do 037) + docs/memória. *(UX — Wave UX-W4, solo; deps: 030–034, 036, 037)* | [035-densidade-cliques/](./035-densidade-cliques/) | 🟢 |
| 036 | Modo comparação A×B no dashboard | Picker "Compare with…" no detail: `SkillCanonicalList` ganha coluna fantasma com deltas por skill (▲ verde/▼ vermelho), deltas de custo ponderado e ₽ no header, mults lado a lado; B fixa enquanto A navega pelo sidebar; deep-link `?compare=`. Complemento profundo da matriz (032). *(UX — Wave UX-W3; deps: 031, 033)* | [036-comparacao-classes/](./036-comparacao-classes/) | 🟢 |
| 037 | Performance: cache de validação + índices | **Causa da lentidão atual:** toda navegação roda `ValidateAndBuild` (deep clone + InventoryBuilder) p/ as 11 classes ×2 (prerender) — `ListClassFiles` é chamado por lista/detail/edit. Fix: cache de `ClassFileEntry` por arquivo (mtime; invalidação no Save/Delete/Create), índices lazy no `CatalogService` (busca/clothing/categorias), recompute do edit com throttle + 1 chamada de custo, avaliar `prerender:false`. Medição antes/depois obrigatória. *(UX — Wave UX-W0, PRIMEIRO)* | [037-performance-cache/](./037-performance-cache/) | 🟢 |
| 039 | Fundação do balanceamento (modelo + arquétipos + snapshot) | Modelo de 2 orçamentos ancorados no peso (custo 28–32 + **netMult** = Σ(fator−1)×peso; **meta ~+6, Médico = padrão**; regra anti-furo + ressalva de viabilidade peso-baixo), arquétipos das 11 classes (conjunto plausível por classe) e o snapshot de baseline. Entregue: [docs/balance-model.md](../docs/balance-model.md) + [docs/class-archetypes.md](../docs/class-archetypes.md) + `scripts/{skill-weights,class-balance-snapshot}.mjs`. Baseline 2026-06-13: Médico +6.17 (padrão), demais +1.36→+3.43 (subir). *(Balance — Fase 0)* | [039-balance-fundacao/](./039-balance-fundacao/) | 🟢 |
| 040 | Rodada de balance: Suporte/Sobrevivência | Sobrevivencialista subido a **+5.94** (Médico = padrão intacto). Assinatura Immunity ×2.0; Endurance/StressResistance ativadas; Search removida. Diferenciado do Médico (resiliência passiva vs. cura ativa). [Registro](./040-balance-suporte/040-balance-suporte-round.md). Validação in-game pendente. *(Balance — Rodada **1ª**)* | [040-balance-suporte/](./040-balance-suporte/) | 🟢 |
| 041 | Rodada de balance: Recon/Stealth | Batedor **+5.36** (assinatura Perception/Endurance ×3) + Furtivo **+5.33** (CovertMovement/MagDrills ×3) — hierarquia Perception↔CovertMovement invertida (anti-clone). Peso baixo → teto de buff ×3.0 na assinatura. [Registro](./041-balance-recon-stealth/041-round.md). *(Balance — Rodada)* | [041-balance-recon-stealth/](./041-balance-recon-stealth/) | 🟢 |
| 042 | Rodada de balance: Combate | Fuzileiro **+5.64** (balística: Assault/AimDrills/RecoilControl/MagDrills ×2) + Op. Tático **+5.50** (físico/versátil, **sem debuff** = "sem fraqueza"). Anti-clone por magnitude (Assault pico só no Fuzileiro). [Registro](./042-balance-combate/042-round.md). *(Balance — Rodada)* | [042-balance-combate/](./042-balance-combate/) | 🟢 |
| 043 | Rodada de balance: Utilitário/Hideout | Armeiro **+5.33** (WeaponTreatment/TroubleShooting/Crafting ×2 — TroubleShooting peso 2.5 = alavanca) + Gerente **+4.23** (hideout XP ×3 + Shotgun ×1.5 autodefesa; **piso temático** por decisão do usuário — classe fora-de-raid; Memory removida). [Registro](./043-balance-utilitario/043-round.md). *(Balance — Rodada)* | [043-balance-utilitario/](./043-balance-utilitario/) | 🟢 |
| 044 | Rodada de balance: Caçador (sniper) | Caçador **+5.47** (Sniper/AimDrills/Endurance/CovertMovement/Perception ×2). **Correção da pesquisa:** Sniper = bolt-action ≠ DMR; Caçador é bolt-action puro → DMR **não** ativada (não é intercambiável). [Registro](./044-balance-cacador/044-round.md). *(Balance — Rodada)* | [044-balance-cacador/](./044-balance-cacador/) | 🟢 |
| 045 | Rodada de balance: Saqueador (loot) | Saqueador **+5.24** (assinatura Attention/Perception ×3 — velocidade real de loot; +Strength/Endurance ×2 carga). Memory removida (skill morta no EFT 0.14.5). [Registro](./045-balance-saqueador/045-round.md). *(Balance — Rodada)* | [045-balance-saqueador/](./045-balance-saqueador/) | 🟢 |
| 046 | Automação profile→classe (outfit/equipados/stash/dinheiro) | `scripts/extract-from-profile.mjs`: lê um profile SPT e preenche outfit + loadout.equipped + stash + dinheiro (capado) numa classe, com **merge cirúrgico** (preserva skills/skillMultipliers/balance). Cópia fiel da árvore de mods (não casa preset); munição derivada; grava repo+install (+.bak). Exemplo aplicado: profile `6a25db…85ea` → Caçador. *(Tooling)* | [scripts/extract-from-profile.mjs](../scripts/extract-from-profile.mjs) | 🟢 |
| 047 | Roster 11→6 (aplicar matriz) | Trocar os 11 `.jsonc` pelos 6 do redesign + Peladão (Médico/Fuzileiro/Caçador/Fantasma/Saqueador/Tanque) com a matriz recalibrada (skills + skillMultipliers + loadout 🎒 + hideout 🏠). Remove as 6 aposentadas; cria fantasma/tanque; mantém Peladão. Sincroniza `SkillWeights.cs` (3 gems). Só camada 🎯. Coordena c/ editor. *(Redesign — R-W0)* | [047-roster-6-classes/](./047-roster-6-classes/) | 🟡 |
| 048 | ~~Infra de skill custom (padrão SE)~~ | **DESCOPADO** (2026-06-21): decisão de não ter skill custom que escala — tudo vira perk flat. A infra de reviver `ESkillId`/`DisplayList` não é mais necessária. | [048-skill-custom-infra/](./048-skill-custom-infra/) | ⚫ |
| 049 | ~~Skills custom de classe (🧪)~~ | **FUNDIDO no 050** (2026-06-21): todas as signatures são perks flat. | [049-class-custom-skills/](./049-class-custom-skills/) | ⚫ |
| 050 | Perks + drawbacks de signature (🔧🔻) | **Todos os perks E drawbacks por classe**, flat e keyed na classe (`Info.GameVersion`), configuráveis no F12: Combat Medic · Cool Under Fire + Adrenaline · Sharpshooter + Iron Lungs · Ghost Step + Execution · Quick Hands + Silent Looter + Pack Mule · Bulwark + Bunker; **drawbacks** Shaky Hands · Loud Operator · Rooted · Rattled · Overladen · Heavy Frame. Fonte: [class-design.md](../docs/class-design.md). Patch-points + fatiamento 050.0–050.4 mapeados (recon 2026-06-21). *(Redesign — R-W1; deps: **054 prereq do 050.0**, 047 soft)* | [050-signature-patches/](./050-signature-patches/) | 🟡 050.0–050.4 compilam (~21 efeitos); pendente validação in-game + Combat Medic (transpiler), Quick Hands (server), Iron Lungs sway |
| 051 | Levers da zona stances (🔧⚠️) | Caçador/Iron Lungs (dreno de braço-ADS) + Tanque/Bunker (stamina arma pesada) — território do stances mod. Compor via `StaminaController.Multipliers`/`ArmStaminaCoordinator` (recon 2026-06-21). *(Redesign — R-W2; deps: 050)* **IMPLEMENTADO 2026-07-04** (hook de dreno no stances + bridge; cards sem "em breve"; aguardando gate). | [051-stances-zone-levers/](./051-stances-zone-levers/) | 🟡 |
| 052 | Validação in-game das 6 classes | Editor web + launcher + raid (FIKA): matriz/custos, perks 🔧 observáveis, drawbacks 🔻 mordem. *(Redesign — R-W3; deps: 047–051)* | [052-validacao-in-game/](./052-validacao-in-game/) | ⚪ |
| 053 | UI — aba "Perks/Drawback" na tela de Skills | Aba nova na tela de SKILLS listando os perks 🔧/drawbacks 🔻 **da classe ativa**, **bilíngue (EN + pt-br)**, fundada no **design system do jogo** (reusa estilo da tela de Skills, como os `Skills*Patch` de identidade). Client-side. Fonte: [class-design.md](../docs/class-design.md). *(Redesign — R-W3; deps: 050)* | [053-perks-ui-tab/](./053-perks-ui-tab/) | 🔵 painel compila (MVP; aba real = refino) |
| 054 | Rename Ghost/Fantasma → Stealth/Furtivo (impl) | **🔴 Pré-requisito do 050.0.** ✅ código feito (`fantasma.jsonc`→`furtivo.jsonc`: `name`/`displayName` `Stealth`/`Furtivo`; `class-matrix.mjs` key/file; cross-check ✅). **Re-sync CONFIRMADO 2026-07-04** (install com `furtivo.jsonc` name=`Stealth`). **Pendente: só validação in-game** (launcher Furtivo/Stealth; perfil novo; gating `Stealth`). *(Redesign — R-W1; deps: —)* | [054-furtivo-rename/](./054-furtivo-rename/) | 🟡 |
| 055 | UI — detalhe da classe no lobby/loading da raid | **Clicar no personagem** de uma classe (lobby / tela de carregamento da raid) abre o **detalhamento completo da classe**: perks 🔧 / buffs / drawbacks 🔻. Irmão do **053** (mesmo conteúdo, outro ponto de entrada). **Reusa o `PerksCatalog`** (dados bilíngues EN/pt-br, já criado no 050) + design system do jogo. Client-side. **Escopo (055): só a classe LOCAL no loading FIKA** (per-player = 057). *(Redesign — R-W3; deps: 050)* | [055-class-detail-lobby/](./055-class-detail-lobby/) | 🟡 |
| 058 | Ativar masteries inertes ("skills fantasmas") | Tornar **funcionais** as 5 maestrias de arma que aparecem na tela de Skills mas são INERTES (`globals.json` `SkillsSettings: []`): **SMG, LMG, HMG, Launcher, AttachedLauncher**. Duas pernas: (1) **ganho de XP** ao usar a arma da categoria (popular globals server-side se o engine ler, OU patch client por `weapClass`); (2) **efeito por nível** (recuo/ergo). Decidir relação com o perk **Bunker** (050 — bônus flat alternativo): coexistir / substituir / virar elite-bonus. Coop-safe (XP+efeito locais). ⚠️ risco: `[]` pode ser intencional do BSG (mecânica hard-disabled na 0.16.x) → spec-tech confirma no assembly se globals é consumido ou se exige patch client. Fonte: globals.json:35261/35376/35482/35483/35559. **IMPLEMENTADO 2026-07-04 (gate V rodado em perfil zerado): SMG/LMG/GL sobem VANILLA (anti-XP-duplo — fora da perna de XP); underbarrel = única morta → XP POR DISPARO via `FirearmController.method_57` + efeito por nível (recuo/ergo) pras categorias alcançáveis; HMG DEFERIDA (só estacionária). F12 `Weapon Mastery`. Aguardando gate in-game (asbuild §Validação).** *(feature nova; deps: —, relaciona 050)* | [058-ativar-masteries-inertes/](./058-ativar-masteries-inertes/) | 🟡 |
| 057 | Identidade de classe **per-player em coop (Fika)** | Hoje o ícone/cor/tooltip da classe só aparece no **player local**. **Entregue (2026-07-03, escopo restrito ao LOADING FIKA por decisão do usuário):** rota server `/customclasses/class-identities` (mapa nickname→classe de todos os perfis; Edition é a chave do `ClassVisualRegistry` — matching en/pt server-side, hipótese `GameVersion`-no-client descartada: perfis remotos indisponíveis no loading) + client `ClassIdentities` (refetch por tela) + popover 055 per-player + tint do nickname. Raid scav local = no-op; scav remoto pode exibir classe do PMC (limitação documentada). Superfícies lobby/nametag/chat = item futuro. **Aguardando gate in-game (coop 2+ players).** *(Identidade — itens 011–015; deps: —)* | [057-class-identity-coop/](./057-class-identity-coop/) | 🟡 |
| 056 | UI — avaliar indicador de perk em outros stats visíveis | **Generalização da Feature 1 (peso↔Pack Mule):** mapear onde o jogo já mostra um **número que um perk/drawback modifica** e decidir quais merecem o **indicador ▲/▼ + tooltip** (mesmo padrão `SkillPanelPatch`/`PerksCatalog`). Candidatos: ergo/recuo no painel de arma (Sharpshooter/Shaky Hands/Adrenaline), fome/sede (Heavy Frame), ADS (Sharpshooter/Iron Lungs), etc. Recon: só o **peso ↔ Pack Mule** vale (resto = número do item, não bufado, ou não exibido). Implementado escopo mínimo (marcador ▲ +X% no peso). *(Redesign — R-W3; deps: 050, Feature 1)* | [056-perk-stat-indicators/](./056-perk-stat-indicators/) | 🟡 |
| 059 | Catálogo de propriedades atômicas + fix da aba CLASS | Refatora o `PerksCatalog` em **propriedades atômicas** (`PerkGroup`/`PerkLine`; perk/drawback + valor **derivados do multiplicador + polaridade**), **display em 2 colunas** (perks esquerda / drawbacks direita) + fix da aba CLASS (título "(ícone) CLASS" + posição). Client-side, só exibição. Insumo: plano `shimmying-dreaming-wilkes.md`. *(Redesign — R-W3; deps: 050, 053)* | [059-catalogo-propriedades-atomicas/](./059-catalogo-propriedades-atomicas/) | 🟡 |
| 060 | Weapon Mastery — transparência in-game do efeito por nível | A tela SKILLS não mostra buff nenhum pras 4 maestrias do 058 (os quadradinhos vêm de config do jogo que segue vazia) → o jogador não tem como saber que nível dá −0.4% recuo/+0.2% ergo (RN-04 da review de regras de negócio do 058). Menor mecanismo: card "Weapon Mastery" (classless) na aba CLASS (059) ou badge no painel das 4 skills via `SkillPanelPatch`. *(deps: 058)* | — | ⚪ |
| 061 | Saqueador — Quick Hands (busca 2 itens de uma vez) | Conceder ao Saqueador, **desde o início**, a busca simultânea de 2 itens ao revistar. ⚠️ **Anotação (usuário, 2026-07-05): esse comportamento JÁ EXISTE vanilla como bônus ELITE da skill Search (nível máx. 51 — buff `SearchDouble`)** — o item é antecipar o bônus pra classe, não criar mecânica nova. Spec-tech decide o mecanismo: (a) conceder o elite-buff da Search no perfil/sessão vs (b) patch client no fluxo de search; definir no-op quando a skill atingir elite naturalmente (sem dobrar) e coop-safety (efeito local). Card `quick_hands` já existe no `PerksCatalog` (pending) — implementação remove o ⏳. *(deps: 050; relaciona 052 validação)* **ENTREGUE 2026-07-11** — mecanismo **(b) client**, mas no LEVER, não no fluxo: Postfix no getter `SkillManager.IsSearchDouble` (ref: SkillManager.cs:1843), **único consumidor** = `CanStartNewSearchOperation` (GClass2235.cs:82 → `count < 2` com o buff, `count < 1` sem). Forçar o getter reusa TODA a lógica vanilla (inclusive o teto de 2). Server SPT não tem lógica de `SearchDouble` (só locale) → sem re-validação. **No-op no elite natural** (sai cedo se `__result` já true) · **coop-safe** (gate no `SkillManager` do MainPlayer; bots usam `BotSearchControllerClass`). F12: `Quick Hands — Enabled` (seção 6 · Scavenger). Card sai do "em breve". [`QuickHandsPatch.cs`](../modded/Client/Patches/QuickHandsPatch.cs) | — | 🟢 |
| 062 | Baseline v2 — loadouts a partir dos perfis novos | Perfis SPT novos (1/classe, criados pelo usuário) viram a FONTE dos loadouts. **Pipeline corrigido (2026-07-06):** extrator pina `x/y/rotated` do stash (builder já honrava — item 038; fallback auto-pack), `LoadAmmo` enche carregador AVULSO (raiz da linha), rublos NORMALIZADOS (300k fixo, stacks do perfil descartados), Scabbard copiado, políticas SecuredContainer=Alpha p/ todos + Pockets TUE/Unheard no Saqueador, `DEFAULT_EXCLUDE` (DSP `62e910aaf957f2915e0a5e36`). Plano: `~/.claude/plans/fluffy-finding-stonebraker.md`. **Re-extração das 6 EXECUTADA (2026-07-06, perfis novos do usuário): posições pinadas, mags avulsos carregados (4–21/classe), Alpha, TUE no Saqueador, 300k, zero DSP; snapshot de skills intacto. Pendências: faca do Furtivo (6 tpls fora da DB local, mod do server oficial) · Saqueador sem faca no perfil · gate in-game.** *(deps: —; precede o balance B1–B19)* | — | 🟡 |
| 063 | Rota pública de classes p/ launcher | `GET /customclasses/classes` (StaticRouter, sem sessionId): lista `Enabled && Registered` com `editionKey` exato + displayName/description en-pt + iconUrl (static files) + nameColor + skills/mults. Contrato SP0 congelado no [004 do launcher](../../../launcher/Launcher4.0beta/backlog/004-classes-dados-reais/). Desenhada p/ servir também o **057** (identidade coop). ⚠️ toca `modded/Server/`. | [058-launcher-classes-route/](./058-launcher-classes-route/) | 🟢 |

> **Pré-requisito de infra (000 — fora do sandbox do mod):** ✅ **feito em 2026-06-07.** [.agents/scripts/compile-mod.sh](../../../.agents/scripts/compile-mod.sh) agora detecta vários `.csproj`, classifica cada um (client/server/lib), builda os entry projects e instala **só as DLLs próprias** do mod (filtra SPTarkov/Unity/BepInEx/NuGet) nos 2 destinos (server → `SPT/user/mods/`, client → `BepInEx/plugins/`). Verificado: syntax + classificação contra SkillDistribution/Skills-Extended. Build dotnet end-to-end pendente até existirem projetos (item 001).

## Épico: editor web de classes (018–029)

> Plano aprovado em 2026-06-09 (`~/.claude/plans/`, sessão 2026-06-09). Editor Blazor Server **dentro do mod** (padrão Skills-Extended: `IModWebMetadata` + `SPTarkov.Server.Web`), servido pelo server SPT em `https://localhost:6969/customclasses`. Fonte de verdade = `.jsonc` do **install** (volta pro repo via `/sync-classes`, item 019). Custo = fórmula RZ pura (XP-mults fora). Briefs de kickoff em cada pasta (`*-00-kickoff.md`) — insumo pro `/create-spec`.

```
W0: [018]                       ← doc; alimenta todas as specs
W1: [019] → [020] · [021] [022] ← 019 (pequeno) merge PRIMEIRO; 020 inicia depois; 021/022 paralelos desde o início
W2: [023] [024]                 ← Web/Shared/ × Web/Pages/
W3: [025]                       ← solo (cria o shell de abas que W4/W5 estendem) — fecha o MVP
W4: [026] [027]                 ← aba Equipado + ItemSpecEditor × criar/duplicar/deletar
W5: [028] [029]                 ← aba Stash × docs
```

Waves executáveis por sub-agents em paralelo (worktree isolado quando tocam a mesma árvore `Web/`).

## Épico: UX do editor (030–037)

> Origem: comparação de UX com o viewer de perfis do RZ (`tools/tarkov-itemdb/viewer/profiles.html` + `profiles-skills.html`), que resolvia melhor: tudo de um perfil numa tela, skills em ordem canônica fixa (comparação imediata), sidebar persistente (1 clique) e tooltips no hover. Os itens portam esses padrões para o editor Blazor sem perder o que ele tem a mais (edição, hot-apply, custos vivos, diagnostics). Revisado em 2026-06-10 (12 achados de UX endereçados + revisão de DESEMPENHO: lentidão atual diagnosticada → item 037 vira a primeira wave).

```
UX-W0: [037]         ← PERFORMANCE primeiro: cache de dry-run + índices (a lentidão atual; 030+ consomem o cache)
UX-W1: [030] [031]   ← sidebar (Layout/NavMenu) × skills canônicas (Shared + Detail/Edit) — territórios disjuntos
UX-W2: [032] [033]   ← matriz (página nova) × dashboard (ClassDetail) — disjuntos
UX-W3: [034] [036]   ← loadout visual (GearPanel/StashPanel/coluna direita) × comparação A×B (header/SkillCanonicalList) — disjuntos
UX-W4: [035]         ← solo: densidade global + cliques + preferências + regressão Chrome MCP (toca todas as páginas)
```

Tarefas-alvo (cliques): ver skills de outra classe **1** (hoje 2+ com perda de contexto); comparar todas as classes **0** depois de abrir a matriz (hoje impossível); comparar 2 classes em profundidade **2** (036; hoje só com 2 abas do browser); ver custo/equipado/stash de uma classe **0** no dashboard (hoje até 4 expansões — Stash/Mults/Hideout/Outfit nascem fechados — + scroll). Desempenho-alvo (037): navegação entre vistas **sem nenhum dry-run** com cache quente (hoje 11 dry-runs ×2 por navegação), medido antes/depois.

## Épico: balanceamento das classes (039–045)

> Plano aprovado em 2026-06-13 (`~/.claude/plans/`). Processo rigoroso e reutilizável de rebalance, ancorado no **peso de skill** (`SkillWeights.cs`). Dois orçamentos: **custo** (skills iniciais, 28–32) e **multiplicador** (`netMult` = Σ(fator−1)×peso). **Meta (decisão do usuário): netMult ~+6 para todas, com o Médico de Combate como padrão intacto** — elevar todas ao nível do Médico, diferenciando por *quais* skills cada uma acelera (não pelo tamanho do net). Fundação completa no 039 ([balance-model.md](../docs/balance-model.md) + [class-archetypes.md](../docs/class-archetypes.md) + scripts). Peladão **isento** (`noBaseline`).

```
039: fundação (modelo + arquétipos + snapshot/baseline)   ← FEITO
040: Suporte/Sobrevivência (Sobreviv.; Médico = padrão)    ← 1ª rodada
041: Recon/Stealth (Batedor + Furtivo)        ⚠ peso baixo
042: Combate (Fuzileiro + Op. Tático)
043: Utilitário/Hideout (Armeiro + Gerente)   ⚠ Gerente peso baixo
044: Caçador (sniper) — solo, ativar DMR
045: Saqueador (loot) — solo                  ⚠ peso baixo
```

Cada rodada (040–045) = `/deep-research` do papel → proposta (`skills` + `skillMultipliers` mirando ~+6, anti-furo, anti-clones) → coordenar fonte de verdade com a sessão do editor → aplicar via `build-class-jsons.js --force` + `/sync-classes` → validar (`class-balance-snapshot.mjs` + `check-skill-costs.mjs` + matriz/A×B + smoke in-game). **⚠ Peso baixo:** Gerente/Saqueador/Recon não chegam a +6 com teto ×2.0 → sub-decisão por rodada (teto de buff maior / piso documentado / skill temática de peso maior), levada ao usuário.

## Épico: redesign das classes 11→6 (047–052)

> Aprovado em 2026-06-20 (handoff `customclasses-class-redesign`). Reduz o roster para **6 classes de identidade forte** (Médico/Fuzileiro/Caçador/Furtivo/Saqueador/Tanque) + **Peladão isenta** — na prática **7 arquivos** (o "11" contava o Peladão; o "6" são as balanceadas). Arquitetura **"tudo-é-skill-real"** (skills + skillMultipliers + skills custom no padrão SE; sem spawn-buffs efêmeros). Design consolidado em [docs/class-design.md](../docs/class-design.md) (class-overview/class-levers **arquivados** em `docs/.archived/`; [class-skill-catalog.md](../docs/class-skill-catalog.md) = fórmulas); matriz reproduzível em [scripts/class-matrix.mjs](../scripts/class-matrix.mjs) (cross-check ✅; topo ~+6 / base ~+4). **Supersede** o épico de balance 11-classes (039–045; `balance-model.md`/`class-archetypes.md` anotados como histórico do método).
>
> **Configurabilidade (decisão #8):** todo lever 🔧/🧪 exposto no **F12** (BepInEx); matriz 🎯 no editor web; server-side com nota de "restart para aplicar 100%". Detalhe avaliado na Fase 5. Parâmetros em [class-design.md](../docs/class-design.md) (seção F12).

```
R-W0: [047]            ← config: roster 6 + matriz + SkillWeights.cs (coordenar editor). A camada 🎯 já funciona sozinha → valida cedo.
R-W1: [048] [050]      ← infra skill custom (SE) × patches de signature 🔧 — disjuntos (050 não depende de 048)
R-W2: [049] [051]      ← skills custom 🧪 (sobre 048) × levers zona stances (sobre 050; ⚠️ gate: decidir coordenar-vs-trocar ANTES do /create-spec 051 — e antes do 050 se tender a "trocar")
R-W3: [052]            ← validação in-game (FIKA) — depende de todos
```

> ⚠️ R-W1/R-W2 paralelos tocam a mesma árvore `modded/Client/Patches/` → **worktree isolado** (ou sequencial), como nos épicos anteriores.

Camadas: 🎯 skill (matriz, vanilla boostada) · 🔧 perk per-player (flat, keyed na classe) · 🎒 loadout (gear) · 🏠 hideout. *(🧪 skill custom foi descontinuada em 2026-06-21 — tudo virou perk flat, ver [class-design.md](../docs/class-design.md).)* Pontas soltas vivas ([class-design.md](../docs/class-design.md)): sync do `SkillWeights.cs` (047), levers da zona stances (051), bug do Círculo de Cultistas (decisão no 047).

> **Convenção de viewer (a partir do 047):** o editor mostra só skills **funcionais** — o dump de skills inertes ("fantasmas": SMG/HMG/Launcher/AttachedLauncher/Auctions/Bot*/etc.) foi removido do `SkillCanonicalList`, e as 6 gems funcionais viraram a seção canônica **"Gems (SE)"** no `SkillMaster.cs`. *(2026-06-21: como não há mais skill custom 🧪, o `SkillMaster` não ganha categoria nova.)* Perks (🔧) não são skills → documentar em [class-design.md](../docs/class-design.md) + expor no F12 (decisão #8); não entram no viewer de skills.

## Legenda

- ⚪ Backlog · 🟡 Em progresso · 🟢 Entregue · 🔴 Cancelado

## Fluxo

1. `/add-backlog-item <mod> <descrição>` → cria entrada + invoca `/create-spec`
2. `/create-spec <ref>` → spec funcional (critérios de aceite + corner cases)
3. `/review-spec <ref>` → editor crítico da spec funcional
4. `/create-technical-spec <ref>` → pré-código com refs ao Assembly/SPT
5. `/review-technical-spec <ref>` → cria review-NN.md (incremental); resolver até zerar
6. `/code-mod <ref>` → implementa em `modded/`
7. `/compile-mod <ref>` → build + instala no SPT
8. `/code-review <ref>` → revisão do build
| 064 | Caçador — Stalker (ruído de movimento −20%) | Perk NOVO (pedido do usuário, 2026-07-11): o Caçador produz menos barulho ao se mover — **irmão mais fraco do Ghost Step** do Furtivo (Caçador ×0.80 = −20%; Furtivo ×0.70 = −30%), preservando o Furtivo como dono da furtividade. Implementado no helper `QuietStep.MultFor()` (`ClassSoundPatches.cs`), que centraliza o ruído REDUZIDO por classe e alimenta os **3 pipelines de som** (rolloff `method_67`, IA base `BotEventHandler.PlaySound`, SAIN `PlayAISound`) — espelha o `LoudOperator.MultFor()` (efeito oposto). F12: `Stalker — Enabled` + `Stalker — Sound radius mult` (seção `4 · Hunter`); card `stalker` no catálogo com `live:` do F12. ⚠️ **Balance:** o board de 2026-07-05 tratava o Caçador como "equilibrado — classe de referência" e NÃO considerou este perk (poder adicionado sem contrapartida) — reavaliar (alavanca sugerida: Rooted 0.85→0.75). ✅ **Coop:** vale para peers Fika nos DOIS canais — percepção da IA (B14/item 065) e rolloff+volume que humanos ouvem (B20/item 066). *(deps: 050.4)* | — | 🟢 |
| 065 | Coop — perks de som valem para peers Fika (B14) | Os BOTS vivem no processo do HOST, então é o host quem calcula o que a IA ouve — inclusive do barulho de um peer. Os patches de som gateavam no player LOCAL, o que tornava **placebo** contra a IA todo perk sonoro de quem joga como CLIENTE: Ghost Step (Furtivo −30%), Stalker (Caçador −20%), Loud Operator (Fuzileiro/Tanque +30%) e Silent Looter (Saqueador). **Fix sem protocolo novo:** o host resolve a classe de QUEM EMITIU o som (`ClassIdentities.ClassNameEnOf` — local via `SkillMultipliers`, peer via o mapa nickname→classe da rota 057) e aplica o multiplicador DELA. Helpers `QuietStep`/`LoudOperator`/`SilentLooter` ganharam `MultFor(classe)`; `AiSoundPatch` (`BotEventHandler.PlaySound`) e `SainSoundPatch` (SAIN) passaram a usá-lo. ⚠️ **Bots barrados por `IsAI`** — eles TAMBÉM emitem passo por esse pipeline e o EFT gera nome de bot de uma lista de nicknames reais (colisão daria a classe de um jogador a um bot). Prefetch do mapa no `OnGameStarted` (o GET é síncrono; sem ele o 1º passo daria hitch). ~~**Fora de escopo:** o rolloff audível (`method_67`) — o som que VOCÊ ouve de um peer exigiria sync real.~~ → **premissa FALSA, retratada pelo item 066** (o `method_67` de um peer roda no cliente de quem ouve; não há protocolo envolvido). ⚠️ O VALOR sai do F12 do HOST (ele é a autoridade da IA). *(deps: 057, 050.4)* | — | 🟢 |
| 066 | Coop — rolloff de peer + volume do passo (B20) | **Retrata o 065.** O som que VOCÊ ouve de um peer agora respeita os perks DELE — e **sem protocolo**: `ObservedPlayer : FikaPlayer : LocalPlayer : Player`, e o `ObservedPlayer` até sobrescreve `ProtagonistHearing` para usar o `BetterAudio` **local** (ObservedPlayer.cs:137) ⇒ o `method_67` de um peer roda **no cliente de quem ouve**; o passo sai por `Player.PlayStepSound` (público, sem gate de local). `SoundRadiusPatch` trocou o gate MainPlayer por `ClassIdentities.ClassNameEnOf(__instance)` — mesma peça do B14. **Bug de meio-caminho corrigido junto:** o patch antigo mexia só no ALCANCE; o **VOLUME** do passo vem de `method_64 → method_69 → _cachedMovementRolloff`, e `method_67` grava esse cache **antes** de qualquer Postfix — então o Postfix nunca o alcançava. O patch passou a espelhar o valor no cache (via `FieldRef` cacheado), que é exatamente o que o `Physical.SoundRadius` do vanilla faz (sobrepeso afeta alcance **e** volume). **Do code-review do 065:** `SkillMultipliers.Prefetch()` no raid-start (a classe local não era refetchada, só o mapa → trocar de classe no editor web deixava você stale e os peers frescos); `ClassNameEnOf` deixou de chamar `EnsureLoaded` (evita GET **síncrono** na main thread a cada passo); guard de hideout movido para ANTES dos prefetches; e o mapa do server passou a incluir o nickname do **SCAV** (quem entra de scav carrega o nickname do scav — sem isso, o peer scav não resolvia; bots-scav do mesmo pool de nomes seguem barrados pelo gate `IsAI`). *(deps: 065, 057)* | — | 🟢 |
| 067 | F12 — editar a COR de cada classe | Pedido do usuário (2026-07-12): poder trocar a cor do nome da classe pelo **F12**, sem editar `.jsonc` nem passar pelo editor web. **Hoje** a cor nasce no server (`nameColor` do `.jsonc` → `ClassVisualRegistry` → rotas `skill-multipliers`/`class-identities`) e chega ao client como string hex em `ClassIdentities.Identity.NameColor`, consumida por chat (`ChatSpecialIconPatch`), tela de loading (`ClassDetailLoadingPatch`), painel de grupo (`PartyPlayerItemPatch`) e tooltips. **Desenho proposto:** (1) usar `ConfigEntry<Color>` (UnityEngine.Color) — o ConfigurationManager tem **color picker nativo**, muito melhor que digitar hex; (2) **ponto de intercepção ÚNICO**: transformar `Identity.NameColor` num resolver `override F12 → fallback server`, mesmo padrão do `PerkLine.Live` (B4) — assim os ~6 call-sites pegam de graça, sem tocar em nenhum patch de UI; (3) sentinela de "usar a cor do server": `alpha == 0` **ou** um toggle `Override class colors` (decidir na spec); (4) a cor de cada classe vai na **seção da própria classe** (`2 · Combat Medic` … `7 · Tank`), coerente com a reorg do F12. **Decisão (usuário, 2026-07-12): o Peladão GANHA seção própria** → `8 · Naked`, e `Vanilla Skill Fixes` passa a `9 ·`. ⚠️ **BREAKING**: renomear uma seção descarta o valor salvo no `.cfg`, então as **4 props** de Vanilla Skill Fixes (Weapon Mastery Enabled · Underbarrel XP per shot · Recoil bonus per level · Ergo bonus per level) voltam ao default para quem já customizou — comunicar no changelog. ⚠️ **Coop:** a cor é **local** (cada jogador vê a sua) — não sincroniza, e não deve mesmo: é preferência visual, não regra de jogo. ⚠️ A cor do server segue sendo o **default**; o F12 só sobrescreve. **Entregue v0.3.0 (2026-07-17, aguarda validação in-game):** desenho **toggle + color picker POR CLASSE** (decisão do usuário — alpha-sentinela e toggle-global descartados). Cada seção de classe (`2·Medic`…`7·Tank` + nova `8·Naked`) ganhou `Override color` (default OFF) + `Class color` (`ConfigEntry<Color>`, color picker nativo do CM; default = cor atual do server). Reorg BREAKING aplicada: Vanilla Fixes `8·`→`9·` (as 4 props resetam — changelog). Resolver `ClassColorOverride.Resolve(classNameEn)` (novo arquivo) espelha o B4 (`PerkLine.Live`): sentinela = toggle, keyed por NameEn EN. Colocado no **acessador** de `NameColor` (`SkillMultipliers` + `Identity`) → TODOS os consumidores pegam de graça (chat, menu+**AccentColor do Menu-Overhaul**, loading, party, tooltips, `PerksPanelView`), inclusive os gates de string crua. `EnsureColorConverter` (TomlTypeConverter #RRGGBBAA idempotente — BepInEx não traz Color). Update **ao vivo no menu** via `ClassColorsChanged`→`MenuClassIdentityPatch.RefreshColors`. Docs PROPERTIES/PROPRIEDADES sincronizadas. Code-review adversarial independente: **0 bloqueadores** (achados A/B/D aplicados: dict `OrdinalIgnoreCase` + limitação de rename documentada, comentário/docs stale, coroutine órfã). ⚠️ Limitação: F12 cobre as 7 classes shipped — rename de `displayName.en` via editor web herda a cor do server. *(deps: 057, 011)* | — | 🟡 |
| 068 | Peladão — texto de MÉRITO na aba CLASS e no tooltip (bilíngue) | Pedido do usuário (2026-07-12): o Peladão precisa de um texto próprio que **celebre** o que ele é — a classe mais **raiz / thug** do jogo, sem buff nenhum, começando sem nada — em vez de aparecer como "classe sem perks". Vale para a **aba CLASS** (`SkillsClassTabPatch`, item 053) e para o **tooltip** (`ClassIdentityView.BuildTooltip`), em **EN e PT**. ⚠️ **Achado que define o escopo:** a `description {en,pt}` do `.jsonc` **NÃO chega ao client hoje** — ela alimenta só o launcher (`DescriptionLocaleKey` + locale) e não existe em nenhum payload (`SkillMultipliersResponse`/`ClassIdentitiesResponse`). Então o item precisa **(1)** expor a descrição numa rota (campo novo no payload, seguindo o molde de `classNameEn/classNamePt`) e **(2)** consumi-la na aba/tooltip — não dá para "só trocar o texto". A descrição atual do Peladão ainda é **placeholder** (`"(placeholder — review skin/items)"`), então será reescrita junto. Decidir na spec: o texto de mérito é um **campo novo** por classe (ex.: `flavorText`) ou a própria `description` reaproveitada? **Entregue v0.4.0 (2026-07-17, aguarda validação in-game):** decisão do usuário = **reusar `description`** (sem campo novo; vale pra todas as classes). Plumbing server→client espelhando `DisplayNameEn/Pt`: `description {en,pt}` agora viaja em `ClassVisualRegistry.Visual` → `SkillMultipliersResponse` (local) + `ClassIdentitiesResponse` (por-nickname) → `SkillMultipliers.Description`/`Identity.Description` (resolvido por idioma). Consumo: aba CLASS (`PerksPanelView` ganhou elemento `FlavorText` italic abaixo do header) + tooltip (`BuildTooltip` anexa 2ª linha). Peladão (sem perks) agora mostra o **texto de mérito** em vez de "classe sem perks" (empty-state distingue `identity!=null`=mod-sem-perks de vanilla; card redundante suprimido quando há descrição). Description do Peladão reescrita (era placeholder) — ⚠️ **texto é DRAFT, aguarda wording final do usuário** (é config, troca sem rebuild). Code-review adversarial independente: **0 bloqueadores** (2 achados aplicados: `<noparse>` contra injeção de rich-text em prosa; supressão do card redundante). ⚠️ Config diverge repo↔install (furtivo/fuzileiro/medico/saqueador/tanque) — pré-existente, `/sync-classes` pendente; `peladao.jsonc` editado repo+install cirurgicamente. *(deps: 053, 057)* | — | 🟡 |
| 069 | Review COMPLETO de bilinguismo (EN/PT) | Pedido do usuário (2026-07-12): varrer o mod inteiro e garantir que **todo texto visível ao jogador** exista em **inglês e português** — F12, aba CLASS, tooltips, notificação de raid-start, cards de perk/drawback, nomes e descrições de classe, editor web e mensagens de UI. Hoje a cobertura é **parcial**: o F12 usa descrições bilíngues numa string só (`"PT / EN"`, porque o ConfigurationManager fixa as strings no `Awake`, antes do EFT carregar o locale — ver 057/PROPRIEDADES), o `.jsonc` tem `displayName`/`description` `{en,pt}`, mas há textos soltos em PT ou EN puro espalhados pelos patches. Entregar: inventário do que está fora do padrão + correção + regra escrita de como adicionar texto novo. *(deps: 008, 067, 068)* | — | ⚪ |
| 070 | `/review-mod-properties` + tooltips EN/PT em todas as props | Pedido do usuário (2026-07-12): rodar a skill **`/review-mod-properties CustomClasses`** (revisão crítica de UX/organização do F12 — ordem e nome de seções, alocação, tipo/faixa, props mortas, uso de Advanced) e, junto, garantir **tooltip bilíngue em TODAS as propriedades**. É o fechamento natural do 067 (seção nova do Peladão + cores) e do 069 (bilinguismo). Fazer **por último**, para a review incidir sobre o F12 já reorganizado — rodar antes seria retrabalho. *(deps: 067, 068, 069)* | — | ⚪ |
| 071 | Aba CLASS — remover o ruído de "weapon mastery" (texto vanilla) | Pedido do usuário (2026-07-12): a aba **CLASS** mostra um texto sobre as **skills vanilla de weapon mastery**, que não tem nada a ver com a classe e polui a tela. **Causa provável (evidência):** o texto **não é do mod** — não existe menção a mastery no `PerksCatalog` nem no painel. O `SkillsClassTabPatch` apenas faz `_panel.SetActive(true/false)` (SkillsClassTabPatch.cs:30/36): ele **desenha o painel da classe por cima**, mas **não desliga** os elementos que a tela nativa de Skills já renderiza — então um TMP nativo (a descrição de mastering) continua visível por baixo/ao lado. **Fix:** ao ativar a aba CLASS, esconder os elementos nativos que não pertencem a ela, e restaurá-los ao voltar para SKILLS/MASTERING (guardar o estado anterior — não deixar a tela nativa quebrada). ⚠️ Confirmar in-game qual TMP é (o patch já tem um diagnóstico que dumpa caminho/texto/alpha de cada TMP da aba — SkillsClassTabPatch.cs:399-404). **Entregue v0.2.5 (2026-07-15, aguarda validação in-game):** causa confirmada na DLL real — o toggle-group nativo (`GClass3808.SelectTab`) só SELECIONA a aba default (SKILLS), nunca a MASTERING, então o mod nunca escondia `_masteringScreen` (a única sub-tela que ele não tocava), cujo texto estático de weapon mastery vazava atrás do painel da CLASS. Fix simétrico ao `skillsScreen.Close()`: `masteringScreen?.HideGameObject()` no build (o grupo re-exibe a tela ao clicar MASTERING — `GClass3807.Show → MasteringScreen.Show → ShowGameObject`). **Diagnóstico F12** novo (`DumpNativeTexts`, gated em `Perk Diagnostics`): loga TODOS os TMPs ativos da tela (`[071-screentext]` path/alpha/text) — se o fix não bastar (resíduo for OUTRO TMP), o log acusa o caminho exato num só teste. Code-review adversarial independente: 0 bloqueadores (2 achados menores aplicados). ⚠️ Fix é a HIPÓTESE principal; validação in-game confirma. *(deps: 053)* | — | 🟡 |
| 072 | Perks pendentes do 050 (B12) — Calm Sights · Rapid Care · Swift Surgeon | Fecha os perks que o **050 deferiu** e que o painel ainda mostra como **"soon"** (`pending: true` no `PerksCatalog`). **(1) Calm Sights / Mira Serena** (🎯 Caçador) — *sway* **×0.7**; alvo: `MotionEffector.SwayFactors` (MotionEffector.cs:28), recalculado por `ProceduralWeaponAnimation.UpdateSwayFactors()` (chamado em MovementContext.cs:1935) → **Postfix** (escrever direto seria sobrescrito). **(2) Rapid Care / Cuidado Rápido** (🩺 Médico) — tempo de cura/estabilização **×0.7**. **(3) Swift Surgeon / Cirurgião Ágil** (🩺 Médico) — tempo de cirurgia **×0.5**. ⚠️ O 050 deferiu (2) e (3) porque a duração é **variável local** em `ActiveHealthController.DoMedEffect` → exige **transpiler** OU o par `HealthEffectsComponent.UseTimeFor` + `FirearmsAnimator.SetUseTimeMultiplier` (**casar efeito e animação**: patchar só um dessincroniza cura e animação). **Preferir o par de patches** — transpiler é último recurso (`csharp-mod-best-practices` §3). ⚠️ ~~**Mobile Surgery FICA DE FORA**~~ → **ENTREGUE também.** A premissa "não localizável no estático" era **falso-negativo do decompile quebrado** (102 namespaces vazios — `EFT.HealthSystem` entre eles). Descompilando a DLL real, a cadeia apareceu inteira: `DoMedEffect` → `MedEffect.Started` → `Player.OnHealthEffectAdded` (Player.cs:28939) liga `EPhysicalCondition.HealingLegs` quando o efeito reporta `NoMove`, e `HealingLegs` é o **único** motivo de `CanWalk == false` → `IdleStateClass.Move()` descarta o input. O perk desliga só `HealingLegs` e **mantém** `UsingMeds` (anda, mas não corre/pula). **Nem o transpiler foi preciso**: `HealthEffectsComponent.UseTimeFor` é público. **Entregue em 2026-07-13 (v0.2.0), pendente de validação in-game.** *(deps: 050; board: B12)* | — | 🟢 |
| 073 | Renomear "perk/drawback" → "buff/debuff" (mod inteiro, EN+PT) | Pedido do usuário (2026-07-15): trocar a terminologia **perk/drawback** por **buff/debuff** em todo o mod, nas **duas línguas**. ⚠️ **Distinguir 2 camadas** (medido 2026-07-15): **(A) OBRIGATÓRIO — texto VISÍVEL ao jogador**: F12 (descrições PT/EN), aba CLASS, tooltips, notificação de raid-start, cabeçalhos do painel de cards ("Class perks / drawbacks" em `PerkDiagnostics.cs:173`, "Perk Diagnostics"), e qualquer string EN/PT. **(B) DECISÃO — identificadores de código internos** (~348 ocorrências em `.cs`: `PerksCatalog`, `PerkGroup`, `PerkLine`, `PerksConfig`, `RaidPerksNotificationPatch`, `PerkDiagnostics`, `PerkDiag`…): renomear é um refactor grande e arriscado, de baixo valor (código é EN por convenção e "buff/debuff" também é EN) — **recomendo NÃO renomear os identificadores** nesta passada, só o texto visível; decidir na spec. ⚠️ **Semântica:** "perk/drawback" (permanente por classe) é mais preciso que "buff/debuff" (efeito temporário/status) — mas é escolha de produto do usuário, respeitada. ⚠️ **F12 é BREAKING** se uma `key` de propriedade mudar (não só a descrição) — manter as keys, mudar só o texto descritivo. Casa com o **069** (bilinguismo) e **070** (review de props) — fazer junto. *(deps: 069, 070)* | — | ⚪ |
| 074 | Perks/drawbacks inertes ou clampados — auditoria de eficácia + fixes (ex-"Rooted/Execution", code-review 050.1) | Achado no code-review do fix de decaimento (v0.2.2, 2026-07-15): ao rerotear os 3 perks de velocidade pelos getters `MaxSpeed`/`SprintingSpeed`, **Heavy Frame (−10% walk) funciona**, mas **Rooted e Execution ficaram inertes** — o getter `MaxSpeed` é lever só pra BAIXO e só acima do teto de mira. **F1 — Rooted (Caçador, −15% ADS):** a velocidade em ADS é limitada pelo TETO DE MIRA (`ClampSpeed`/`StateSpeedLimit` ≈0.33–0.50, MovementContext.cs:1843 + Player.cs:12155), menor que o MaxSpeed reduzido (≈0.61) → o `min()` ignora o Rooted. Lever certo: `AimMovementSpeed`/`StateSpeedLimit` de mira. **F2 — Execution (Furtivo, +10% melee):** a velocidade real tem teto ABSOLUTO no `SpeedLimiter`/`CharacterController.SpeedLimit` (`GClass2175`, Run 4.6 m/s, GClass2175.cs:216-221) que NÃO lê MaxSpeed → o +10% é clampado. Lever certo: levantar o cap do SpeedLimiter (⚠️ tipo obfuscado, mais frágil). **Nota:** ambos já estavam quebrados ANTES (dentro do decaimento) — não é regressão nova, mas segue não-funcional. **F3 (menor):** o `+1f` da fórmula de sprint torna a redução sublinear (≈−3–5% em vez de −10%) e variável com Str/stamina — dívida de calibração. **F4 (perf):** `MaxSpeedPatch`/`SprintingSpeedPatch` são Postfix em getter QUENTE que roda para TODA instância (bots/peers) antes do early-out — cachear a ref do MainPlayer.MovementContext no raid-start. **🔎 AUDITORIA DE EFICÁCIA (2026-07-18, espelho do 075 mas p/ "o efeito CHEGA ao resultado?"):** varredura dos ~24 efeitos → **3 INERTES + 2 PARCIAIS**, o resto EFETIVO. Além de F1/F2/F3: **F5 — Overladen (Saqueador, inércia ×1.5) INERTE por ALVO ERRADO (AP-03):** o Postfix está em `BasePhysicalClass.OnWeightUpdated`, mas `PlayerPhysicalClass` sobrescreve SEM chamar base (~PlayerPhysicalClass.cs:950) → nunca roda pro player. Fix LIMPO: mirar `typeof(PlayerPhysicalClass)`. (⚠️ `MoveSideInertia`/`MoveDiagonalInertia` = campos mortos, sem consumidor vivo; só `Inertia` importa.) **F6 — Rattled (Furtivo, aim-punch ×1.5) PARCIAL por CLAMP:** `Clamp01` em `ForceEffector.AddForce:126` corta o ×1.5 em hits fortes (satura 1.0; só entrega cheio em dano pré-armadura <~59). Gêmeo do bug do MaxSpeed; assimétrico (Cool Under Fire ×0.5 pra baixo passa ileso). Fix: escalar o `strength` cru ANTES do `sqrt/Clamp01` (em `EffectsController`). **Levers confirmados na DLL real:** Rooted → `MovementContext.SetAimingSlowdown(true, 0.33+AimMovementSpeed)` (MovementContext:1831/1843, FirearmController:12155); Execution → `GClass2175` (CONFIRMADO presente, GClass2175:216-221) via `Config.PlayerStateLimits[Run/Sprint].MaxSpeed`. **Ressalvas ergo (menores, NÃO inertes):** boost de ergo (Bunker/Mastery) não melhora hip-fire (`HipInaccuracy` lê campo cru, FC:12866) e satura em ergo=100. **Insight balance:** quase todos os quebrados são DRAWBACKS (Overladen/Rooted/Rattled/HeavyFrame-sprint) → contrapartidas não mordem → classes acima do alvo do board. **PLANO (atacar 1×1, gate de instância [[reference_customclasses_perk_gating]]/075 + validação in-game em CADA):** **Lote 1** (limpos, não-obfuscados) = Overladen → Rooted → Rattled · **Lote 2** (frágil) = Execution-vel (`GClass2175`) · **Lote 3** (calibração) = `+1f` do sprint. **ENTREGUE v0.4.1 (2026-07-18, aguarda validação in-game — os 5 fixes, cada um com review adversarial independente = 0 bloqueadores):** **F5 Overladen** retarget `BasePhysicalClass`→`PlayerPhysicalClass.OnWeightUpdated` (AP-03; re-trigger de raid-start via OnWeightLimitsUpdated já cobre o spawn-timing; limitação: só a inércia LIVE, não as acelerações derivadas — documentada). **F1 Rooted** novo `RootedAimSlowdownPatch` (Prefix em `SetAimingSlowdown`, escala o `slow` do teto de mira ×0.85). **F6 Rattled** o multiplicador migrou de `strength` (clampado por `Clamp01`) p/ `hands`/`camera` (não clampados) no `AimPunchPatch` — +50% cheio em todo hit; Cool Under Fire idem (melhora hits enormes). **F2 Execution** novo `ExecutionSpeedCapPatch` (Postfix em `GClass2175.method_1` ×1.10, cap do SpeedLimiter; Enable com try/catch defensivo p/ o obfuscado); branch MaxSpeed mantido p/ o andar. **F3 sprint** `ClassMoveSpeed.Apply` ganhou `isSprint` e compensa o `+1f` (`patched = m·V + (m−1)/S`, S=`Physical.SprintSpeed`) → num2 recebe o fator EXATO (fecha F3 e casa o cap do Execution). **F4 (perf)** os getters seguem com o early-out de instância (auditoria 075 confirmou barrado antes de mutar) — cache do MainPlayer deferido (custo desprezível). Todos com gate de instância (bots do host intocados). *(deps: 050.1; regra 075)* | — | 🟢 |
| 075 | Auditoria — perks/drawbacks vazando para AI/scav (relato: scav tankando tiro) | Relato de jogador (2026-07-17): um **SCAV às vezes "tanka" tiros** que não deveria — suspeita de efeito de classe (redução de dano) aplicado indevidamente a AI ou a personagem scav. **Varredura pedida:** auditar o gating de TODO patch de EFEITO de perk/drawback, verificando vazamento para **(i)** bots AI, **(ii)** o próprio jogador entrando de **SCAV** (a classe é do PMC — deve valer no scav raid?), **(iii)** peers Fika. **Levantamento inicial (2026-07-17):** os patches de efeito TÊM gate — `MainPlayer` + `IsLocalClass`/`IsYourPlayer` (Bulwark [BulwarkPatch.cs:50-58], Adrenaline, CalmSights, ClassCombatHealth, ClassMedic, ClassMovement, ClassSound, ClassWeapon, PackMule, QuickHands, RecoilFloor, WeaponMastery). Os SEM gate são só UI (chat/menu/notificação — não afetam combate). Logo **no nível do patch o bot AI parece excluído**. ⚠️ **Hipótese líder:** o gate `IsLocalClass("Tank")` lê a classe do **PMC** (`SkillMultipliers.ClassNameEn`); num **scav raid** o `MainPlayer` é o scav mas o `SkillMultipliers` pode continuar reportando a classe do PMC → o **Bulwark** (dano ×0.85) e outros efeitos aplicariam ao personagem scav do jogador. Decidir (produto): perk de classe deve valer no scav raid? Se não, gatear também por "é PMC raid". **Suspeitos primários:** `BulwarkPatch` (única redução de dano direta) e `ClassCombatHealthPatches` (health/dano — auditar o que faz + gate exato). **Cross-ref:** 074/F4 nota que `MaxSpeedPatch`/`SprintingSpeedPatch` (Postfix em getter quente) **rodam para bots/peers** antes do early-out — confirmar que o early-out realmente barra o efeito, não só o cálculo. ⚠️ **Coop:** conferir resolução de `MainPlayer` no HOST (bots rodam no host) vs client — memória `feedback_coop_multiplayer_sync`. **Entregável:** tabela `perk × alvo-pretendido × pode-vazar-para{AI, scav-player, peer}` + correção dos gates furados + regra escrita de gating de efeito. **CONCLUÍDO v-code (2026-07-17) — escopo reduzido a bots do HOST (scav-player fora, decisão do usuário):** varredura de TODOS os patches de efeito (Bulwark, ExecutionMelee, Adrenaline, LocalHitType, metabolismo, IronLungs, anti-jam, CalmSights, ClassMovement Max/Sprint/Overladen, ClassWeapon Shoot/reload/ADS/ergo/aim-punch, ClassMedic UseTime/anim/MobileSurgery, PackMule, QuickHands, RecoilFloor, WeaponMastery, sons 065/066). **0 leaks para bot do host** — cada patch confirma a identidade da instância (`ReferenceEquals(__instance, MainPlayer…)` / `IsYourPlayer` / `ProfileId` do atacante) OU barra `IsAI` via `ClassNameEnOf` (sons), ANTES de mutar. O anti-padrão "`IsLocalClass` sozinho mutando `__instance`" NÃO ocorre. Getters quentes de movimento (074/F4) rodam pra toda instância mas o early-out `ReferenceEquals(ctx, MainPlayer.MovementContext)` barra bot antes de qualquer mudança. **Conclusão:** o "scav tankando tiro" NÃO vem de perk do mod em bot — sobra o caso scav-**player** (fora de escopo) ou armadura/RNG vanilla. **Regra de gating de instância escrita** em [class-design.md](../docs/class-design.md) (seção Implementação) p/ travar leaks futuros. *(toca 050/065/066; sem deps diretas)* | — | 🟢 |
| 076 | Médico — cirurgia sem reduzir o HP MÁXIMO do membro curado (⚠️ sync Fika) | Pedido do usuário (2026-07-18): a cirurgia do Médico (CMS/Surv12) NÃO deve reduzir o **limite máximo de HP** do membro curado no PACIENTE (vanilla: restaurar um membro destruído deixa o máximo daquele membro reduzido pelo resto da raid). Perk de assinatura do Médico (soma-se a Swift Surgeon/Mobile Surgery do 072). **Ponto de interceptação (recon 2026-07-18):** o vanilla chama `ActiveHealthController.RestoreBodyPart(bodyPart, healthPenalty)` com `healthPenalty = Random.Range(damageEffect.HealthPenaltyMin, HealthPenaltyMax)/100f` (ActiveHealthController real ~L1907) — é o **2º arg** que corta o máximo. Perk = **zerar/anular o `healthPenalty`** quando o OPERADOR é Médico. ⚠️ **O gate é a classe do MÉDICO (operador), NÃO do paciente** — resolver via `ClassIdentities.ClassNameEnOf(operador)` / rota 057 (nickname→classe), não `IsLocalClass` do paciente. **⚠️ SYNC FIKA — cercar os 3 cenários** (health é autoritativo no cliente do PACIENTE, com espelho `ObservedHealthController` nos outros processos): **(a)** médico HOST → paciente CLIENT · **(b)** médico CLIENT → paciente HOST · **(c)** médico CLIENT → paciente CLIENT. Para cada um definir: em QUAL processo `RestoreBodyPart` roda (o do paciente?) e como esse processo conhece a classe do médico. **Infra que já existe:** o mod já lida com médico operando em ALIADO — `ClassMedicPatches` (o prefix do aliado chama `DoMedEffect` no HealthController do PACIENTE, ClassMedicPatches.cs:128-131); a rota 057 já mapeia nickname→classe de peers. **Aplicar a regra de gating de instância** ([[reference_customclasses_perk_gating]], item 075) — RestoreBodyPart roda pra qualquer health controller (inclusive bot). ⚠️ **Verificar:** `ObservedHealthController` tem/roda `RestoreBodyPart`? Se a cirurgia em paciente remoto restaura no cliente do paciente e só espelha, o patch precisa estar no processo certo — este é o miolo do item. **ENTREGUE v0.5.0 (CustomClasses) + v1.2.3 (ICM), 2026-07-19, aguarda validação in-game:** perk **"Restorative Surgery"** (F12 `2 · Combat Medic`: Enabled + Max HP penalty mult, default 0 = sem cicatriz). **Arquitetura opção B** (decisão do usuário): CustomClasses expõe `CombatMedicSurgery.Adjust(doctor, penalty)` + `SetExternalHandling`; o **ICM (TRL-ImmersiveCombatMedicine) é o mod OFICIAL de cura de aliado** (Band-Aid NÃO fica ativo) e chama a API por reflection (soft-dep, fail-open). **Auto-cirurgia** = patch nativo `SurgeryPenaltyPatch` no `ActiveHealthController.RestoreBodyPart` (operador=paciente). **Ally** = ICM ajusta em `ApplySurgery` (#2, doctor conhecido → penalty ajustado viaja no packet) e no full-treatment do receiver (#3, doctor resolvido do `DoctorProfileId`), com `SetExternalHandling` p/ o patch nativo não re-ajustar pela classe do PACIENTE. Os 3 cenários coop cobertos; gate = classe do OPERADOR (`ClassNameEnOf`, barra IsAI). ⚠️ **Semântica corrigida no review (BLOQUEADOR):** o `healthPenalty` é a FRAÇÃO RETIDA (não a penalidade) — a fórmula virou `Lerp(1, penalty, mult)` (mult 0 → retém 100%). Review adversarial: 1 bloqueador (a fórmula) + 1 forte (comentário do overload; a regen de membro com penalty=1f fica inócua pós-`Lerp`) — ambos aplicados. **Bug lateral achado no 072:** o `BandAidIsRedirecting()` referencia `Band_Aid.MedicHealPatch` — verificar se o `MedicHealPatch` do mod oficial está mesmo no namespace `Band_Aid` (o `MedicalLogic` está), senão o guard não dispara. **v0.5.1 (2026-07-19):** registrado no `PerksCatalog` (grupo Médico, `Flag` "Restorative Surgery" / "Cirurgia Restauradora") — a v0.5.0 tinha o mecanismo + F12 mas o perk NÃO aparecia na aba CLASS / tooltip / notificação de raid-start. **REVIEW #2 (adversarial independente) + validação in-game parcial (2026-07-19):** arquitetura e fórmula **reconfirmadas**; `Lerp(1,penalty,mult)` **provada pelo decompile real** (AHC `RestoreBodyPart` L3902-3903: `Maximum × healthPenalty`; skill empurra p/ 1). **Reentrância do `_externalHandling` (bool) FECHADA:** o único assinante síncrono de `BodyPartRestoredEvent` (disparado dentro do escopo) é o `TraumaEngine` do próprio ICM, e o handler só marca dirty (defere p/ o Update) — sem re-entrada. **Bug lateral do 072 = FALSO ALARME:** `MedicHealPatch` ESTÁ em `namespace Band_Aid` e `IsRedirectingHeal` é `public static` → o guard do 072 dispara (NÃO abrir item). **✅ AC-6 coop VALIDADO IN-GAME** (LogOutput.log da raid 2026-07-19, L4382-4383): host `Teste3` (**sem classe**) operou o Médico-cliente → cirurgia recebida com **Penalty 66% (VANILLA)**, não 100% — o `SetExternalHandling` + gate pelo operador funcionaram, o perk NÃO vazou. **⚠️ Achados novos do review#2 (dívida técnica):** **(N1, premissa coop NÃO-documentada)** o ajuste da cirurgia de aliado roda no **processo do PACIENTE** (`ApplyFullTreatmentLocally` chama `CustomClassesBridge.AdjustSurgeryPenalty`) → o perk exige **CustomClasses instalado no cliente do PACIENTE**, não só no do operador (senão fail-open→vanilla). Casa com a premissa "config idêntica p/ todos" mas precisa estar escrito como pré-condição de AC-3/4/5. **(N2, assimetria de gate, baixa)** Médico operando BOT local pelo **redirect nativo do MedEffect** gate-eia pela classe do PACIENTE (bot=null→vanilla; satisfaz AC-7 por acaso); só o fallback programático raro (`MedicalLogic.ApplySurgery`, quando `DoMedEffect` falha em TODAS as partes) gate-eia pelo operador → um Médico operando bot aplicaria o perk (viola AC-7, edge raríssimo/cosmético). **(N3, compat, baixa)** SPT-Realism-Mod/RealismMod co-patcham `RestoreBodyPart`; SAIN chama `RestoreBodyPart(part,1f)` (bot self-surgery) — inócuo sob o 076 (`Adjust(bot,1f)=1f`). **⚠️ Achado FORA do 076, na mesma raid (candidato a item do ICM):** na cura/cirurgia de **ALIADO** o operador NÃO é imobilizado — o `HealRoutine`/`BandAidController` seta só `EPhysicalCondition.UsingMeds` (BandAidController.cs:561), **nunca `HealingLegs`** (o único bloqueio de andar); só a AUTO-cirurgia cria o MedEffect nativo que enraíza. Logo **qualquer** operador (Médico ou não) ANDA durante a cirurgia de aliado — o "host andou durante a cirurgia" NÃO é o Mobile Surgery vazando. Idem "curou rápido": cura de aliado usa tabela FIXA do ICM (Surv12=20s, Grizzly=5s), e o log prova que nem o Médico-operador acelera (guard `BandAidIsRedirecting` bloqueia o 072 no redirect). Ver [[reference_icm_ally_heal_operator_not_immobilized]]. **v0.6.1→0.6.2 (2026-07-19) — alvo 100%→80% (piso configurável, decisão do usuário):** o vanilla retém pouco (CMS 0.25–0.45, Surv12 0.60–0.72); o perk agora eleva a retenção a um **PISO** do F12 (default **0.80** = 80%; nasceu 0.90 na v0.6.1, ajustado p/ 0.80 na v0.6.2) via `Max(vanilla, piso)` — nunca pior que o vanilla, e a **skill Surgery empurra ALÉM** (80% é mínimo, não teto). F12 `Restorative Surgery — Max HP penalty mult` (0..1, 0=100%) → **`Restorative Surgery — Restored max HP`** (0..1, default 0.80) — ⚠️ **BREAKING** (key renomeada reseta o valor salvo). Descrições atualizadas (F12 + card do catálogo + PROPRIEDADES/PROPERTIES). Fórmula `Adjust` = `Math.Max(penalty, retention)`; regen (penalty=1f) segue inócuo (`Max(1,0.80)=1`). *(deps: 072, 057, 065; regra 075)* | — | 🟡 |
| 077 | Médico — perks de TEMPO/MOVIMENTO valem na cura de aliado do ICM (cross-mod) | Achado na raid coop de 2026-07-19 (ver 076/review#2): os perks de tempo/movimento do **072** foram feitos assumindo a **auto-cirurgia vanilla** e NÃO previram a cura de ALIADO do ICM. **Gaps:** **(1) Mobile Surgery** — na cura de aliado o ICM nunca liga `HealingLegs` (só `UsingMeds`, BandAidController.cs:561), então **TODO** operador anda (não-Médico incluso) → o perk vira placebo e "parece vazado". **(2) Swift Surgeon / Rapid Care** — o guard `MedicTiming.BandAidIsRedirecting()` (ClassMedicPatches.cs:193) bloqueia o 072 no redirect, então **NINGUÉM** acelera na cura de aliado (nem o Médico) — tempo é fixo pela tabela do ICM (`stats.UseTime`, ex. Surv12=20s). **DECISÕES DO USUÁRIO (2026-07-19):** **(A) Andar:** o ICM passa a **imobilizar o operador por padrão** na CIRURGIA de aliado (replica o lock vanilla), e **só o Médico com Mobile Surgery anda**. **(B) Velocidade:** o ICM **aplica os perks de tempo do Médico-OPERADOR** (Swift Surgeon ×0.5 na cirurgia, Rapid Care ×0.7 no curativo) ao seu próprio `UseTime` + animação. **DESIGN (opção B, espelha o 076):** o operador é SEMPRE o MainPlayer LOCAL (só se opera como si mesmo) → **dispensa mapa 057 e packet** (movimento/animação replicam via Fika nativo). CustomClasses expõe 2 APIs bridge-friendly (fail-safe): `AllyHealTimeMult(bool isSurgery)` (fator do operador local Médico via `IsLocalClass`+toggles; reusa `MedicTiming.FactorFor`) e `AllyMobileSurgery()` (bool: Médico local c/ Mobile Surgery on). O ICM (`CustomClassesBridge` + `HealRoutine`): multiplica `stats.UseTime` pelo fator e casa a animação (`SetUseTimeMultiplier = 1/fator`, hoje fixa em 1f); e se `IsSurgery && !AllyMobileSurgery()` seta `HealingLegs=true`, pareando o unset em TODOS os cleanups (fim/EmergencyDrop/CancelHeal/Deactivate/morte/raid — mesma malha do `UsingMeds`). ⚠️ **Riscos:** lock de movimento esquecido num cleanup = médico preso (usar helper pareado); lock só na CIRURGIA (bandagem não enraíza — gate `IsSurgery`); dessincronia efeito↔animação (o par tempo+anim tem que bater, como o 072 já faz na auto); fail-open (CustomClasses ausente → tempo normal + ninguém anda, que é o comportamento correto do ICM standalone). ⚠️ **Reavaliar** se o guard `BandAidIsRedirecting` do 072 ainda é necessário (o ICM passa a controlar o próprio tempo, não o `UseTimeFor` vanilla). **IMPLEMENTADO (2026-07-19, SDD completo — aguarda build + validação in-game):** spec funcional+técnica+review-01 (0 bloqueadores). CustomClasses expõe `CombatMedicAllyPerks` (`AllyHealTimeMult`/`AllyMobileSurgeon`, gate `IsLocalClass`, fail-safe); ICM `CustomClassesBridge` resolve por reflection (fail-open); `BandAidController.HealRoutine` liga `HealingLegs` na cirurgia de aliado exceto Médico+Mobile Surgery, multiplica `UseTime` + animação (`MedicHealPatch.AllyAnimSpeedMult`) pelo fator; helper `ReleaseSurgeryImmobilize` em todos os cleanups. Guard `BandAidIsRedirecting` MANTIDO. Limitação (PA-01-01): aceleração de tempo plena só no path de aliado remoto; parcial no bot local. *(deps: 072, 076; mod ICM)* | — | 🟡 |
| 078 | Rebalance v2 — recalibração de valores (planilha classes-perks-draws) | Épico [rebalance-v2-2026-07-25.md](./rebalance-v2-2026-07-25.md). **Só muda DEFAULT no `PerksConfig` + `.jsonc` (sem código novo).** 8 valores: Swift Surgeon 0.5→**0.75**, Rapid Care 0.7→**0.75** (Medic); Iron Lungs 0.667→**0.7** (Hunter); Adrenaline Focus 0.80→**0.7**, Adrenaline Reload 0.80→**0.7** (Rifleman); Steady Mount 0.85→**0.7**, Tireless Arms 0.20→**0.5**, Heavy Appetite 1.30→**1.15** (Tank). **AC:** cada valor reflete no F12 e no efeito in-game; nenhum outro perk muda. **Corner:** dentro da faixa aceitável do F12. **Sync:** nenhum (local). **ENTREGUE v0.6.3 (2026-07-26):** 8 defaults no `PerksConfig` + descrições atualizadas; `.cfg` da instalação (D:\SPT) sincronizado no mesmo passo (valores customizados do usuário sobrescritos p/ os da planilha — Tireless Arms 0→0.5, Heavy Frame 1.2→1.15). Build 0/0. Aguarda validação in-game. *(deps: 050)* | — | 🟢 |
| 079 | Rebalance v2 — realocação de levers existentes (REMOVER/ADD/rename) | Épico rebalance-v2. Sem mecânica nova — mexe nos GATES de classe (`IsLocalClass`) de cada patch + `ByClass`/`Library` do catálogo + F12. **REMOVER:** Mobile Surgery (Medic) · Overladen (Scav). **ADD (drawback):** Rattled→Medic · Light Frame (carga −0.2)→Hunter+Stealth · Falta de habilidade (recuo 1.25)→Medic+Scav · Saque Barulhento (volume loot ×1.3)→Rifleman. **RENAME:** Shaky Hands/Mãos Trêmulas→**Falta de habilidade** + ligar (OFF→on). ⚠️ **Saque Barulhento = Silent Looter INVERTIDO** (volume >1) → precisa do pipeline de som **coop/AI** (065/066/B14) p/ IA e peers ouvirem mais. ⚠️ rename da key **reseta** valor salvo (changelog). **AC:** cada classe mostra o conjunto novo na aba CLASS; removidos somem; ADD gateados só na classe certa (075 — sem vazar p/ bot/scav-player). **Corner:** estado entre raids; toggle mid-raid. **ENTREGUE v0.7.1 (2026-07-26, g-autodev):** todas as realocações aplicadas (ver [asbuild](./079-realocacao-levers/079-realocacao-levers-05-asbuild.md)). Code-review adversarial por sub-agent: 0 bloqueadores; F1 (tooltip de carga verde/piso p/ Light Frame drawback) + F3/F4 (comentários stale) corrigidos; F2 (Loud Looter compõe com Loud Operator no SAIN → loot ~1.69× p/ a IA) aceito como limitação. Build 0/0. Aguarda validação in-game. *(deps: 050, 065, 066; regra 075)* | — | 🟢 |
| 080 | Rebalance v2 NOVO — Saque Rápido (deploy de arma no slot HOLSTER) | Épico rebalance-v2. Perk NOVO (×0.8) p/ **Hunter + Rifleman + Stealth**: acelera empunhar QUALQUER arma equipada no slot **HOLSTER** (gate pelo SLOT, não pelo tipo de arma). Patch novo na velocidade de saque/deploy. **AC:** sacar a arma do holster ~20% mais rápido só p/ as 3 classes; primária inalterada; não-classe inalterado. **Corner:** troca rápida de slot; holster vazio; arma trocada mid-raid. **Sync:** nenhum (animação local, replica via Fika nativo). **ENTREGUE v0.8.0 (2026-07-26, g-autodev):** Postfix em `Player.FirearmController.GetWeaponDrawSpeedMultiplier` (draw ×1.25), gate MainPlayer+classe+slot Holster. ⚠️ validar in-game (timing do gate Holster). Build 0/0. *(deps: 050)* | — | 🟢 |
| 081 | Rebalance v2 NOVO — Lebre (velocidade quando leve) | Épico rebalance-v2. Perk NOVO (Scav, ×1.3): +velocidade de movimento **quando NÃO está pesado** (abaixo de X% de carga). Patch condicional ao peso. ✅ **Limiar resolvido (2026-07-26):** usar o estado NATIVO de overweight do EFT — `BasePhysicalClass.Overweight == 0` (o "ícone de bigorna" aparece quando `Overweight > 0`). "Não pesado" = sem o ícone; sem inventar %. **AC:** leve (sem bigorna) → +velocidade; ao ficar pesado (bigorna) → normal; só Scav. **Corner:** transição no limiar sem flicker; interação com inércia/Overladen (que será removido no 079); peso muda ao lootar. **Sync:** nenhum (velocidade replica via Fika nativo, molde Heavy Frame). **ENTREGUE v0.9.0 (2026-07-26, g-autodev):** branch no `ClassMoveSpeed.Apply` (Scav + `Physical.Overweight<=0` → ×1.30), sem patch novo. Limiar = overweight nativo (ícone de bigorna). Build 0/0. *(deps: 050, 079)* | — | 🟢 |
| 082 | Rebalance v2 NOVO — Medroso (tremor de mãos sob fogo) | Épico rebalance-v2. Drawback NOVO (Scav): mãos trêmulas sob fogo. **PORTAR o mod `UnderFire-2.0.1`** — detecção por dano recebido (`Player.ReceiveDamage`) + **near-miss** (`GClass897.OnShoot`, geometria <5m) + efeito **`Tremor`** (`AddEffect<T>`), **gateado só p/ Scav**, e **DESATIVAR o UnderFire global** (hoje aplica a TODOS, sempre). ⚠️ **reconfirmar nomes ofuscados** (`GClass3008`/`GClass897`/`GInterfaceNNN`) no decompile ATUAL (o mod é de build antiga — usar `graph-code-navigation`/decompile). **AC:** Scav atingido/sob fogo → tremor por alguns segundos; outras classes e bots sem efeito; UnderFire global off. **Corner:** limpar estado entre raids; cooldown (não empilhar tremor); não conflitar com a Adrenalina do Fuzileiro. **Sync:** nenhum (gatilho e efeito locais ao Scav). *(deps: 050; mod UnderFire; regra 075)* | — | 🟢 |
| 083 | Rebalance v2 NOVO — Morte Silenciosa (faca sem som) | Épico rebalance-v2. Perk NOVO (Stealth): abate com faca **não emite som** + **puxar a faca** não faz barulho. Patch novo de SOM. ⚠️⚠️ **COOP + AI CRÍTICO:** o som do abate/faca **não pode alertar bots (SAIN/vanilla) nem peers (Fika)** — suprimir na origem + não notificar a IA (molde do gate de som 065/066, no pipeline de melee/kill). **AC:** matar bot com faca não alerta bots próximos nem aparece p/ peers; puxar a faca é silencioso; só Stealth. **Corner:** o grito/hit-sound do alvo; faca vs outros melee; matar player humano (coop). **O MAIS CARO do épico — por último**, com gate in-game antes. *(deps: 050, 065, 066; regra 075)* | — | 🟢 |
| 084 | Rebalance v2 NOVO — Recarga Rápida Escopeta (2 cartuchos por vez) | Épico rebalance-v2. Perk NOVO (Tank). **SPIKE primeiro:** confirmar a mecânica elite de **Mag Drills** do EFT (carregar 2 shells por vez) e se dá p/ forçar o bônus (molde Quick Hands/061 = Postfix em getter elite). Fallback se inviável: reduzir o **tempo** de recarga de escopeta. **AC:** Tank recarrega escopeta ~2× mais rápido (2 por vez); só escopetas; só Tank. **Corner:** escopeta com carregador (Saiga) vs tubular; interação com a skill Mag Drills natural do jogador. **Sync:** nenhum (local). *(deps: 050)* | — | 🟢 |
| 085 | Bugfix — Adrenaline Reload (Fuzileiro) estava INERTE | Achado durante o spike do 084 (2026-07-26): o `ReloadSpeedPatch` fazia Postfix em `Player.FirearmController.GetWeaponReloadAnimationSpeed`, que é **código morto** no EFT 0.16.9 (nada o chama — o reload speed virou push-based). O perk "Adrenaline Reload" (−30% tempo de recarga na janela de combate) **nunca teve efeito**, e o 078 recalibrou esse valor (0.80→0.7) sem saber. Ver [[reference_eft_reload_speed_getter_dead]]. **DECISÃO DO USUÁRIO (múltipla escolha):** consertar agora. **ENTREGUE v0.12.0 (2026-07-26, g-autodev):** `ReloadSpeedPatch` migrado p/ o mesmo funil do 084 (Prefix escala `BuffInfo.ReloadSpeed ÷ t` / Postfix restaura, arma+corpo em lockstep). ⚠️ Como abrir/fechar a janela NÃO gera sync sozinho, `AdrenalineState` ganhou um **watcher** (coroutine 1×/janela) que força `FirearmController.SetAnimatorAndProceduralValues()` na abertura e quando a janela expira → o patch re-avalia e aplica/restaura. Coexiste com o 084 no mesmo método (classes exclusivas Tank×Rifleman). Gate MainPlayer+Rifleman+janela (075). Build 0/0. Aguarda validação in-game (recarga acelera na janela e volta ao normal ao expirar). **AC:** Fuzileiro na janela recarrega ~30% mais rápido; fora da janela normal; só Fuzileiro local. *(deps: 050, 078; regra 075)* | — | 🟢 |
| 086 | Bugfix — Saque Rápido (080) só afetava o quickdraw-fast, não o saque normal | Report do usuário (validação in-game 2026-07-26, Caçador): "não senti diferença no saque do coldre". Causa: o `HolsterDrawSpeedPatch` (080) fazia Postfix em `Player.FirearmController.GetWeaponDrawSpeedMultiplier`, que está VIVO mas só é lido no **quickdraw-fast** (double-tap `FastSlotSelection` do holster, Player.cs:10091 e :12648). O saque NORMAL (trocar de slot pela tecla) usa o campo push-based `BuffInfo.SwapSpeed` via `SetAnimatorAndProceduralValues` (Player.cs:12661) — que o patch antigo NÃO tocava. Mesma natureza do getter morto do 085. Ver [[reference_eft_reload_speed_getter_dead]]. **ENTREGUE v0.13.0 (2026-07-26, g-autodev):** `HolsterDrawSpeedPatch` migrado p/ o funil real (Prefix escala `BuffInfo.SwapSpeed ÷ t` / Postfix restaura, arma+corpo em lockstep), gate MainPlayer+classe(Hunter/Rifleman/Stealth)+arma do slot Holster. Cobre o saque comum (o quickdraw-fast já é rápido). Coexiste com 084/085 no mesmo método (mexe em SwapSpeed, eles em ReloadSpeed → sem conflito). Code-review adversarial: 0 bloqueadores (gate de holster mais seguro que o vanilla; sem vazamento; 075 ok). Build 0/0. **AC:** saque da pistola do holster ~20% mais rápido no saque comum; primária inalterada; só as 3 classes. *(deps: 050, 080; regra 075)* | — | 🟢 |
| 087 | Bugfix — Saque Rápido (080/086) acelerava o PUT-AWAY, não o DRAW-IN | Report in-game (validação 2026-07-27, Caçador com Sidearm=tecla 3, quickdraw OFF): "acelerou a SAÍDA da pistola, não o saque". Diagnóstico do decompile: na troca de arma há DOIS controles independentes — **DRAW-IN** (sacar) = `Animator.speed` GLOBAL via `FirearmController.Spawn(animationSpeed)` (Player.cs:13495 → estado SPAWN/GClass2055), sempre 1f no vanilla (saque NUNCA acelera por skill); **PUT-AWAY** (guardar) = float `SpeedDraw` (=`SwapSpeed`) via `SetAnimatorAndProceduralValues`. O 086 escalava `SwapSpeed` → acelerava só o put-away. Prova BSG: GClass2949 (spawn) seta só `Animator.speed`; GClass2944 (put-away) seta `SpeedDraw`. **ENTREGUE v0.13.2 (2026-07-27, g-autodev):** `HolsterDrawSpeedPatch` agora Prefix em `Spawn` escalando `ref animationSpeed` (draw-in); `HolsterDrawResetPatch` (Postfix em `SetAnimatorAndProceduralValues`) restaura `Animator.speed=1f` no fim do saque (WeaponAppeared) — senão a arma operaria acelerada (tiro/reload/idle) até a troca. Flag static `BoostedDraw` (blindada no raid-start). Gate MainPlayer+classe+arma que ENTRA vem do Holster. Code-review adversarial: 0 bloqueadores (timing do reset confirmado; gate 075 ok; 1 🟡 benigno = flag static, blindado). Build 0/0, client deployado. **AC:** apertar Sidearm (saque normal) puxa a pistola do holster mais rápido; put-away e outras armas inalterados; só as 3 classes. *(deps: 050, 080, 086; regra 075)* | — | 🟢 |
| 088 | Saque Rápido — acelerar a TROCA INTEIRA (put-away+transição), não só o draw-in | Feedback in-game (2026-07-27): com só o draw-in (fase 3) acelerado, o começo da troca destoava — o usuário decompôs em 3 fases: (1) saída da arma anterior/put-away, (2) transição, (3) saque/draw-in. Quer acelerar 1 e 2 p/ distribuir o ganho e afrouxar o 3. Diagnóstico: fase 1 = `FirearmController.Drop(animationSpeed,...,nextControllerItem)` — o `animationSpeed` acelera o put-away (Animator.speed global do controller que SAI); fase 2 = encadeamento de callbacks (o Spawn só começa quando o put-away dispara o evento OnWeapOut), SEM timer → acelerar a fase 1 antecipa a fase 2 de brinde. **ENTREGUE v0.14.0 (2026-07-27, g-autodev):** `HolsterPutAwaySpeedPatch` (Prefix em `Drop`) escala `animationSpeed` gateado por `nextControllerItem` (a arma que ENTRA) vir do Holster — gate INVERTIDO vs. a fase 3 (lá a arma do holster é `__instance.Item`). SEM reset (controller que sai é destruído em seguida). Mesmo fator `QuickDrawTime` das duas fases → troca uniforme. Code-review adversarial. Build 0/0, client deployado. **AC:** trocar p/ a pistola do holster acelera a troca INTEIRA (guardar+transição+saque); trocar DA pistola p/ primária inalterado; só as 3 classes. **v0.15.0 (2026-07-29):** o config único virou DOIS sliders p/ calibração fina — `Put-away time (phase 1)` default **0.75** e `Draw-in time (phase 3)` default **0.65** (a fase 2 não tem knob, encurta junto). *(deps: 050, 080, 086, 087; regra 075)* | — | 🟢 |
